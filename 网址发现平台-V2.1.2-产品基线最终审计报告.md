# 网址发现平台 V2.1.2 产品基线最终审计报告

## 审计结论

**PASS（通过）**

V2.1.1 的核心产品方向成立，但上一轮修订仍存在前台功能与后台数据闭环不完整、部分路线遗漏、推荐依据不可审计等问题。本轮完成 V2.1.2 修订后，达到产品基线最终冻结条件。

---

## 一、本轮修订的 6 项重点

### 1. P0 补齐最小管理后台

Phase 1 / P0 明确包含：

- 网站管理
- Category 管理
- Task 管理
- Tag 管理
- 用户提交网站审核队列
- 网站数据纠错处理队列
- 推荐依据维护
- 编辑精选 / 今日推荐管理
- 基础发布状态管理

原则：后台只服务 P0 数据和内容闭环，不提前建设复杂角色体系或工作流。

### 2. 恢复遗漏的后续页面能力

- Phase 2：专题页、榜单/精选集合
- Phase 3：热门模块，仅在真实数据达到条件后上线

以上均不进入 P0。

### 3. 推荐与排序基准明确化

P0 统一使用可解释规则：

1. Task 匹配度
2. Constraint / Feature 匹配度
3. 数据质量
4. 可验证状态信号
5. 编辑精选 / 人工判断

P0 不引入黑盒推荐模型。

### 4. Query Landing Page 验证数据来源落地

验证来源：

- Google Search Console（可用时）
- Bing Webmaster Tools
- 站内搜索日志
- 点击率
- 收藏等行为数据
- 搜索需求稳定性
- 是否能够形成独立内容

默认不批量自动生成 Query Landing Page。

### 5. Tag 四类定义固定

统一使用 `tags + tag_type`：

- `attribute`：属性
- `feature`：功能
- `constraint`：限制/筛选条件
- `scenario`：使用场景

不建立四套 Tag 表。

### 6. 推荐依据持久化并可审计

核心模型：

```text
recommendations
recommendation_evidence
```

系统必须能够回答：

> 为什么这个网站被推荐？

重要推荐应能够追溯其 Task、匹配因素、数据质量、状态信号或人工编辑依据。

---

## 二、P0 最终闭环

### 用户侧

```text
需求
↓
Query 解析
↓
Task + Constraint
↓
候选网站
↓
可解释排序与推荐
↓
网站详情 / 相似 / 替代
↓
点击
↓
收藏 / 再次发现
```

### 数据与编辑侧

```text
SiteIntel 上游数据同步
+ 用户提交
+ 用户纠错
        ↓
最小管理后台
        ↓
审核 / 分类 / Task / Tag
        ↓
推荐依据
        ↓
发布
```

前台与后台形成完整闭环。

---

## 三、SiteIntel 与网址发现平台的最终边界

### SiteIntel

作为上游网站事实数据与检测数据来源之一，可提供：

- 域名基础信息
- DNS / IP / ASN
- SSL / CDN
- 技术栈
- 网站检测
- 快照
- 当前可访问状态
- 技术与基础变化信号

### 网址发现平台

是独立产品，独立拥有：

- 代码仓库
- 前端
- 后端
- 数据库
- 管理后台
- Category
- Task
- Tag
- Recommendation
- Similar / Alternative
- Editorial Curation
- Search Logs
- User Behavior
- Favorites

硬规则：

> 本项目不得直接耦合 SiteIntel 内部数据库结构，必须通过稳定 API、事件或数据同步契约获取上游数据。

---

## 四、优先级与开发阶段最终映射

| 优先级 | 默认阶段 | 内容 |
|---|---|---|
| P0 | Phase 1 | MVP 核心闭环 + 最小管理后台 |
| P1 | Phase 2 | 账号、同步、认领、专题、榜单等 |
| P2 | Phase 3 | 行为数据推荐、热门模块、Query Landing Page |
| P3 | Phase 4 | 个性化、复杂关系、长期能力 |

硬规则：

> 所有 P0 功能必须在 Phase 1 完成并验收。

---

## 五、最终机械验收清单

- [x] Task 与 Query 不再混用
- [x] Constraint 不再作为独立 Task
- [x] P0 与 Phase 1 同步
- [x] 用户提交具备审核后台
- [x] 用户纠错具备处理后台
- [x] 编辑精选具备管理入口
- [x] 数据质量评分有明确输入
- [x] “活跃度”统一替换为可验证状态信号
- [x] Tag 四类定义固定
- [x] Recommendation 具备可审计依据
- [x] Category 与 Task 有 SEO 差异化规则
- [x] Query Landing Page 有明确验证来源
- [x] 推荐系统不再仅以网站数量触发升级
- [x] 导航站是独立产品
- [x] SiteIntel 仅作为上游数据来源，通过 API / 同步契约解耦

---

# 最终结论

**V2.1.2 PASS。**

本轮修订后，不再继续进行产品方向迭代。

下一阶段正式进入：

```text
1. SiteIntel 上游 API / 数据能力审计
↓
2. 网址发现平台技术架构
↓
3. 数据库 Schema
↓
4. 数据同步契约
↓
5. API 契约
↓
6. 前后台信息架构
↓
7. P0 开发任务拆解
↓
8. 开发与验收
```
