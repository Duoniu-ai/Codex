# 网址发现平台 — 技术架构设计 V1.0（候选版）

> 依据《网址发现平台-完整产品规划-V2.1.2-最终冻结版》§23 路线第 2 步执行。
> 上游输入:《SiteIntel 上游数据能力审计报告(架构阶段 1)》— PASS WITH 2 GAPS。
> 本版**不写业务代码、不修改冻结的产品规划**,只把 V2.1.2 翻译为可执行技术架构。
> 状态:候选版(V1.0-rev2,2026-08-17 与《数据库Schema与数据同步契约-V1.0-冻结版》完成字段级基线同步,**未冻结**,同步记录见 #19)。修订后仍作为 API 契约冻结的输入,但暂不进入冻结。

---

# 1. 架构目标

1. **独立产品**:独立仓库、独立前后端、独立数据库、独立管理后台;SiteIntel 仅作上游数据来源之一,经 API + 同步契约接入,绝不直接访问其数据库。
2. **P0 证明闭环**:需求 → Task+Constraint → 候选网站 → 可解释推荐 → 相似/替代 → 点击 → 收藏,全链路可运行、可验收(含最小管理后台与数据纠错闭环)。
3. **可解释优先**:搜索与推荐全部规则化、无黑盒;每个推荐可追溯(recommendation_evidence)。
4. **数据资产自建**:Task/Category/Tag/Relation/Recommendation/User Behavior 全部导航站自有,与 SiteIntel 数据物理隔离。
5. **简单运维**:与 SiteIntel 同技术生态(已验证模式直接复用),单机可部署,Claude Code 可稳定开发。

---

# 2. 系统架构图

```text
┌────────────────────────────────────────────────────────────────────────┐
│                            用户(浏览器/SEO 爬虫)                         │
└────────────────────────────────────────────────────────────────────────┘
                                    │ HTTPS
┌────────────────────────────────────────────────────────────────────────┐
│                         前端(Next.js App Router)                        │
│  ├─ 用户端: 首页 / 搜索 / Task页 / Category页 / 网站详情 / 收藏 / 提交    │
│  ├─ 管理后台: /admin/* (同一 Next 应用的受保护路由区)                    │
│  └─ 渲染策略: 任务页/分类页 ISR(编辑发布时 revalidate)                    │
└────────────────────────────────────────────────────────────────────────┘
                                    │
┌────────────────────────────────────────────────────────────────────────┐
│                    应用/API 层 (Next.js Route Handlers)                 │
│  Public: /api/search /api/tasks /api/websites /api/categories           │
│          /api/submissions /api/reports /api/events                      │
│          (收藏为纯前端本地数据, 无服务端端点, §10.1)                      │
│  Admin : /api/admin/* (ADMIN_EMAILS 白名单 + 登录 session)              │
│  内部  : /api/sync/* (定时任务触发, 仅本机可连)                           │
└────────────────────────────────────────────────────────────────────────┘
                                    │
┌────────────────────────────────────────────────────────────────────────┐
│                       业务服务层 (src/lib/*)                             │
│  query-parser(规则) → task-matcher(alias词典) → constraint-mapper        │
│  → search-service(候选+过滤) → ranker(五因子) → reason-builder(可解释)   │
│  → quality-scorer(数据质量) → sync-service(上游同步) → notifier(变化信号)│
└────────────────────────────────────────────────────────────────────────┘
                                    │
┌────────────────────────────────────────────────────────────────────────┐
│                          数据库 (PostgreSQL 14, 独立库)                  │
│  核心: websites/categories/tags/tasks/relations/recommendations         │
│  状态: website_status/website_snapshots/website_metrics                 │
│  闭环: submissions/reports/curated_picks/user_events/search_logs        │
│  运维: sync_queue/sync_logs/settings/admin_users                        │
└────────────────────────────────────────────────────────────────────────┘

┌────────────────────────────  上游接入  ──────────────────────────────┐
│  SiteIntel (独立部署, 154 服务器, 3003 端口)                           │
│    │  GET /api/v1/report/{domain}?lang=zh  (X-API-Key: navigation-sync)│
│    │  POST /api/v1/analyze                  (X-API-Key, 30/h 硬顶)      │
│    ▼                                                                   │
│  ┌───────────────────────────┐                                         │
│  │  同步服务 (本应用内, DB 队列) │                                       │
│  │  sync_queue 状态机 → 节流(30/h) → 拉取 → 摘要裁剪 → 写入 → hash diff │
│  └───────────────────────────┘                                         │
│    ▼                                                                   │
│  导航站数据库 (website_status / website_snapshots)                     │
└───────────────────────────────────────────────────────────────────────┘

辅助件(P0 不引入独立中间件):
- 缓存: 页面级 ISR + fetch revalidate; 无 Redis
- 搜索: 应用层词典 + PostgreSQL ILIKE 兜底; 无 Elasticsearch
- 队列: 数据库表状态机; 无 Redis/BullMQ
- 定时任务: instrumentation.ts setInterval tick (与 SiteIntel 同款, 已验证)
- 限流: 内存滑动窗口 (与 SiteIntel 同款实现)
```

## 模块职责表

| 模块 | 职责 | 位置 |
|---|---|---|
| 前端 | 渲染用户端与管理端页面;提交/收藏/纠错等交互 | `src/app/**` |
| 应用/API 层 | 参数校验、鉴权、限流、错误码、分页;调用业务服务 | `src/app/api/**` |
| Query Parser | 查询归一化、token 化、停用词 | `src/lib/search/parser.ts` |
| Task Matcher | alias 词典匹配,Top-1 命中 | `src/lib/search/task-matcher.ts` |
| Constraint Mapper | 约束词 → 标签映射 | `src/lib/search/constraints.ts` |
| Search Service | 候选集合 + 过滤 + 兜底搜索 | `src/lib/search/service.ts` |
| Ranker | 五因子加权排序 | `src/lib/ranking/ranker.ts` |
| Reason Builder | 推荐理由模板生成(可解释;实时搜索仅内存生成,持久化由编辑决策触发,§8.6) | `src/lib/ranking/reasons.ts` |
| Quality Scorer | 六维数据质量评分 | `src/lib/quality/score.ts` |
| Sync Service | 上游拉取/裁剪/写入/hash diff | `src/lib/sync/**` |
| Notifier | 收藏更新提醒的查询层(P1,基于账号体系收藏,不基于 visitor_id,§5.3) | `src/lib/sync/notify.ts` |
| 管理后台 | 九类管理页面 + 状态流转 | `src/app/admin/**` |
| 定时任务 | 同步 tick、任务页阈值复核 tick | `src/instrumentation.ts` |

---

# 3. 项目边界

```text
网址发现平台 (独立)
├── 独立 GitHub 仓库            (站点代码 + migrations + 部署脚本)
├── 独立前端 / 后端 / 数据库    (与 SiteIntel 物理隔离)
├── 独立管理后台
└── 自建数据资产:
    Task / Category / Tag / Relation / Recommendation / Curated Picks
    / User Behavior / Search Logs / Submissions / Reports

SiteIntel (上游数据来源之一)
└── 提供: 域名基础信息 / DNS / IP / ASN / SSL / CDN / 技术栈
         / 可访问状态 / 快照摘要 / 变化信号
    接入方式: 仅 SiteIntel API v1 (X-API-Key: navigation-sync)
             + 数据同步契约 (本报告 §4)
    硬规则: 禁止直连 SiteIntel 数据库; 禁止访问其内部路由;
            同步数据只存摘要层, 不存全量 rawData。
```

---

# 4. SiteIntel → 导航站同步架构

## 4.1 同步流程(九步)

```text
1. 种子域名入队      编辑在后台选"同步"或任务页创建时自动入队 → sync_queue(pending)
2. 调度器 tick       先做超时恢复(§4.2),再原子领取下一批待处理项(每 15 分钟,受节流限制)
3. 节流              全局令牌桶 ≤30 请求/小时(v1/analyze 硬顶), 令牌耗尽快照常, 不阻塞其他功能
4. 调用 SiteIntel    首次: POST /api/v1/analyze → 轮询 reportUrl(完整管线分钟级)
                      已有报告: GET /api/v1/report/{domain}?lang=zh
5. 获取报告摘要      只取 IntelligenceReport 的摘要子集(§4.4 裁剪清单)
6. 写入导航站库      摘要 upsert 到 website_status; 业务摘要快照写 website_snapshots(非 SiteIntel 全量,§4.4)
7. 生成 summary_hash 对摘要子集做确定性序列化 + sha256
8. hash diff         新 hash ≠ 旧 hash → 写入结构化变化事件 website_changes
9. 可验证变化信号    UI 只展示: 最近检测时间 / 可访问状态 / 近期变化类型 / SSL 到期
                      禁止"活跃度/业务增长/质量更高"等表述(冻结版 §23.3)
```

## 4.2 队列与状态机

```text
sync_queue:
  normalized_domain (unique) | job_type | status | locked_at | locked_by
  | attempts | max_attempts | next_retry_at | last_error | last_http_status | summary_hash | updated_at

状态: pending → processing → completed
              ↘ retrying(退避到期后自动回 pending 重新可领取)
              ↘ failed(超过最大 attempts, 人工处置: 重置 pending / 标记 skipped)
              ↘ skipped(人工永久放弃)
```

### 任务领取与 Worker Lock(架构语义,防重必读)

1. **原子领取**:每个 tick 只领取一次任务,领取必须是单条条件更新(Compare-And-Set),保证同一任务不可能被两个 tick / 两个 worker 同时领取:

```sql
UPDATE sync_queue
SET status = 'processing', locked_at = now(), locked_by = :worker_id, updated_at = now()
WHERE id = (
  SELECT id FROM sync_queue
  WHERE status = 'pending' AND (next_retry_at IS NULL OR next_retry_at <= now())
  ORDER BY next_retry_at ASC NULLS FIRST, id ASC
  LIMIT 1
)
RETURNING *;
-- 影响行数 = 1 → 领取成功; = 0 → 无可领取任务,或该行已被其他 tick/worker 抢先领取
```

Prisma 事务按此语义实现:条件 `UPDATE`(updateMany 返回 count)== 1 才视为领取成功;count == 0 不做任何处理,直接进入下一次 tick。**不得**用"先 SELECT 再 UPDATE"两段式(存在竞态窗口)。

2. **同任务防重**:任务一旦置 `processing`,不再满足 `status='pending'` 的领取条件;上一次 tick 未完成时,下一次 tick 无法重复领取同一任务。
3. **超时恢复(含 systemd 重启)**:每个 tick 最先执行恢复语句,把执行中超过锁持有期的行放回队列:

```sql
-- 步骤 1:attempts+1 后未达上限 → retrying(退避基于更新后 attempts)
UPDATE sync_queue
SET status = 'retrying', attempts = attempts + 1,
    next_retry_at = now() + retry_interval(attempts + 1),   -- 指数退避,见下
    updated_at = now()
WHERE status = 'processing' AND locked_at < now() - interval '10 minutes'
  AND attempts + 1 < :max_attempts;

-- 步骤 2:attempts+1 后达到上限 → 直接 failed(第 max_attempts 次失败即 failed,无第 6 次)
UPDATE sync_queue
SET status = 'failed', attempts = attempts + 1, updated_at = now()
WHERE status = 'processing' AND locked_at < now() - interval '10 minutes'
  AND attempts + 1 >= :max_attempts;
```

systemd 重启后,所有 `processing` 行的 `locked_at` 必然超过锁持有期 → 自动进入 retrying → 退避到期后重新可领取,实现**断点续跑**。锁持有期(10 分钟)应显著大于单任务最坏执行时长(analyze 轮询为分钟级)。
4. **失败/重试/退避(重试边界冻结,Schema V1.0 冻结版 §9)**:worker 主动失败时 `attempts+1`,用**更新后值**判定:`attempts >= max_attempts` → 置 `failed` 供人工处置;否则置 `retrying` 且 `next_retry_at = now() + min(2^attempts × 60s, 4h)`(attempts 为更新后值);默认 `max_attempts = 5`:**第 5 次失败即进入 failed,不得出现第 6 次**;429 → 记 `last_http_status=429` 并按退避推迟;5xx/网络错 → 退避重试;404(未分析)→ 转 analyze 分支触发;域名非法 → 直接 `failed` 供人工审;**超时恢复同样计入 attempts**(同边界判定)。
5. **幂等**:`UNIQUE(normalized_domain, job_type)` + 摘要 upsert;summary_hash 未变则零写。重复执行无害。
6. **单实例说明**:P0 单实例部署,instrumentation 单进程 tick;锁机制仍保留以防御 tick 重入与重启竞态;未来多实例直接复用 `locked_by` + 原子领取语义,无需架构变更。
7. **API Key 隔离**:Key 只存在于服务器 `.env`(SITEINTEL_API_KEY),前端永不接触;同步端点仅本机可连(见 §11)。
8. **同步节奏与 30/h**:冷启动 500 域名按 30/h ≈ 17h 分批完成,与编辑内容生产并行;**不因限速修改产品架构**(任务书约束)。

## 4.3 同步触发来源

1. 编辑在后台新增/批量导入网站 → 入队;
2. Task 页创建时其候选网站批量入队;
3. 用户提交网站审核通过 → 入队;
4. 手动"立即同步"按钮(后台);
5. 每日 tick 对 `last_sync_at > 24h` 的 active 网站重拉(状态信号保鲜)。

## 4.4 摘要裁剪清单(导航站存储范围)

| 导航站字段 | 上游来源(v1/report) |
|---|---|
| domain | target.domain |
| title / description | overview.title / overview.description |
| summary_text | overview.summaryText(lang=zh) |
| categories_hint | overview.categories(仅参考,导航站分类由编辑定) |
| online / http_status / checked_at | overview.websiteStatus.{online,status,checkedAt} |
| cdn / hosting | infrastructure.cdn.name / hosting.name |
| technologies | technology[].name(仅名称列表) |
| asn_org / country(单值摘要) | infrastructure.ips[](取主 IP 的 asn.org / country,不存 IP 明细列表) |
| ssl_expires_at / ssl_days_remaining | infrastructure.ssl.{validTo,daysRemaining} |
| history_signals | history[](仅 type/severity/detectedAt,不存 previous/current 全量) |

**明确不存**:evidence.rawData 全量、完整检测结果、原始 evidence、DNS 记录明细、IP 明细列表、证书链、redirects、insights 全量(必要时仅存近期事件类型)。只存 asn_org/country 等单值摘要,**绝不建立 SiteIntel 数据库镜像**。这些归 SiteIntel 专业层,导航站详情页"专业信息层"跳转 SiteIntel(冻结版 §10)。

## 4.5 同步日志

`sync_logs`:domain(原始 Host) / normalized_domain(实体关联键) / action(analyze|report|diff) / status / http_status / error / duration_ms / payload_size / created_at。后台 `/admin/sync` 展示,排障用。

---

# 5. 数据流

## 5.1 搜索主链路(用户侧)

```text
GET /api/search?q=免费做PPT的AI网站
  → query-parser: normalize(小写/去空白/去停用词) → tokens
  → task-matcher: alias 词典包含匹配 → Top-1 Task=制作PPT (命中率 ≥85% 验收, §7)
  → constraint-mapper: 免费→tag(免费, constraint), AI→tag(AI, feature)(§7.3)
  → search-service: Task 候选网站集 → constraint 过滤 → 兜底 ILIKE(无 Task 命中时)
  → ranker: 五因子加权(§8) → 排序
  → reason-builder: 实时生成推荐理由(内存计算,默认不落库,§8.6)
  → 响应 + search_logs 落库(query/识别结果/点击)
```

## 5.2 编辑/数据侧闭环

```text
SiteIntel 上游同步 + 用户提交(submissions) + 用户纠错(reports)
  → 最小管理后台(审核/分类/任务/标签/推荐依据/精选)
  → 发布 → 前端展示 → 用户行为(user_events/search_logs) → 指标回看
  → 编辑推荐决策(创建/修改推荐、人工覆盖排序) → 落库 recommendations + recommendation_evidence(§8.6)
```

## 5.3 变化信号链路

```text
同步 tick → 重拉 → 摘要裁剪 → summary_hash → diff
  → 变化 → 写入 website_changes(结构化变化事件,每站 20 条滑动)
  → 前端详情页"近期变化"展示
  → (P1) notifier 基于账号体系收藏,向收藏该网站的用户发更新提醒(P0 收藏为本地数据,服务端无收藏数据,不参与)
```

---

# 6. 数据库 Schema

> 字段标注:**S** = 来自 SiteIntel 同步; **N** = 导航站自有。时间戳 `created_at/updated_at` 全表统一,以下不重复列出。

## 6.1 核心实体(冻结版 §15 全清单 + 导航站辅助表)

### websites

| 字段 | 类型 | 约束/说明 | 来源 |
|---|---|---|---|
| id | text(cuid) | PK | N |
| domain | text | 原始 Host(SiteIntel 输入/展示/日志),**非唯一** | N |
| normalized_domain | text | **unique 实体键**(www 折叠,Schema V1.0 冻结版 §5 规则) | N |
| name | text | 站点名(编辑填写,可覆盖 SiteIntel title) | N |
| slug | text | unique,预留 | N |
| title | text | | S |
| description | text | 一句话描述(编辑维护,首屏核心层) | N |
| summary_text | text | "这个网站是什么"摘要 | S |
| category_id | text | FK→categories.id,主分类单值 | N |
| status | text | enum: `draft/pending/active/archived` | N |
| is_indexed | boolean | SEO 索引位(阈值自动管理) | N |
| data_quality_score | int | 0-100,规则计算(§8.3) | N |
| first_seen_at / last_seen_at | timestamp | 导航站视角 | N |
| created_at / updated_at | timestamp | | N |

### categories

| 字段 | 类型 | 说明 | 来源 |
|---|---|---|---|
| id | text | PK | N |
| name / slug | text | slug unique | N |
| parent_id | text? | FK 自引用(子分类,如 AI工具›图像生成) | N |
| description | text | 分类说明(SEO 承载层) | N |
| sort_order | int | 导航顺序 | N |
| status | text | `active/hidden` | N |
| is_indexed | boolean | 数量阈值达标才置 true | N |

### tags

| 字段 | 类型 | 说明 | 来源 |
|---|---|---|---|
| id | text | PK | N |
| name / slug | text | slug unique | N |
| tag_type | text | enum: `attribute/feature/constraint/scenario`(冻结版 §14,**固定四枚举,禁止新增**,如 business_model/technology/pricing/platform 均不允许) | N |
| group | text? | 可选管理分组维度(如 price/tech/platform),仅后台分组展示用,**绝不替代 tag_type**,不参与匹配语义 | N |
| description | text | | N |
| status | text | `active/hidden` | N |

### website_tags

| 字段 | 类型 | 说明 |
|---|---|---|
| id | text | PK |
| website_id | text | FK→websites,onDelete Cascade |
| tag_id | text | FK→tags,onDelete Cascade |
| | | **unique(website_id, tag_id)** |

### tasks

| 字段 | 类型 | 说明 | 来源 |
|---|---|---|---|
| id | text | PK | N |
| name | text | 用户语言命名("制作PPT") | N |
| slug | text | unique(`ppt-maker`) | N |
| description | text | 编辑撰写的任务说明(SEO 唯一性) | N |
| status | text | `draft/active/pending_review` | N |
| is_indexed | boolean | 阈值自动管理:候选 ≥5 且 ≥3 完整推荐依据 | N |
| candidate_count | int | 派生(定期重算) | N |

### task_aliases(Task 别名词典,独立表;Schema V1.0 冻结版 §2.6)

| 字段 | 类型 | 说明 |
|---|---|---|
| id | text | PK |
| task_id | text | FK→tasks |
| alias | text | 别名原文(展示) |
| alias_normalized | text | **全局 unique**,归一化别名(识别匹配用) |
| source | text | `manual/rule` |

**词典语义(与冻结版一致)**:tasks 的别名由独立表承载,启动时全量载入内存词典(任务量 30-50,规模极小),后台编辑保存时热更新;alias_normalized 全局唯一防 Top-1 冲突。Task Query 识别逻辑与 Top-1 ≥85% 验收不变。

### task_related_tags(Task 隐含约束/特征/场景提示,独立表;Schema V1.0 冻结版 §2.7)

| 字段 | 类型 | 说明 |
|---|---|---|
| id | text | PK |
| task_id | text | FK→tasks |
| tag_id | text | FK→tags |
| source | text | `manual/rule` |

**UNIQUE(task_id, tag_id)**。用途:**仅辅助搜索意图识别与 Constraint Mapper**;**不替代 website_tasks**(那是"网站能完成该任务"的事实关系)。

### website_tasks

| 字段 | 类型 | 说明 | 来源 |
|---|---|---|---|
| id | text | PK | N |
| website_id | text | FK→websites | N |
| task_id | text | FK→tasks | N |
| match_basis | text | 匹配依据文案(推荐理由素材) | N |
| source | text | `manual/rule` | N |
| sort_weight | int | 默认 50(0-100) | N |
| override_score | int? | 人工固定分(§8.5) | N |
| | | **unique(website_id, task_id)** | |

### website_relations

| 字段 | 类型 | 说明 | 来源 |
|---|---|---|---|
| id | text | PK | N |
| website_id_from / website_id_to | text | FK→websites | N |
| relation_type | text | enum: `similar/alternative/related` | N |
| basis | text | 判定依据(人工或规则说明) | N |
| source | text | `rule/manual` | N |
| confidence | int | 0-100 | N |
| status | text | `active/superseded` | N |
| | | **unique(website_id_from, website_id_to, relation_type)** | |

### website_snapshots(导航站侧同步后的网站事实摘要快照,冻结版 §7"原始检测快照"在导航站的落地)

**语义定义**:本表是导航站同步服务从 SiteIntel 报告裁剪出的**业务摘要快照**,不是 SiteIntel 全量原始检测数据的镜像。SiteIntel 的 evidence.rawData / 完整检测结果 / 原始 evidence 仍属 SiteIntel(§3);导航站只存业务需要的摘要字段(checked_at / online / status / http_status / ip·asn·geo 单值摘要 / technologies 名称列表 / ssl 有效期摘要 / cdn / hosting / seo-health 摘要 / summary_hash,裁剪清单见 §4.4)。

| 字段 | 类型 | 说明 | 来源 |
|---|---|---|---|
| id | text | PK | N |
| website_id | text | FK→websites | N |
| type | text | `status/summary/tech` | N |
| data | jsonb | 业务摘要快照(§4.4 裁剪清单;非 SiteIntel 全量,无 evidence.rawData) | S 派生 |
| hash | text | 确定性 hash | N |
| created_at | timestamp | 索引(website_id, type, created_at) | N |

### website_status(当前聚合状态,冻结版 §7 语义)

| 字段 | 类型 | 说明 | 来源 |
|---|---|---|---|
| id | text | PK | N |
| website_id | text | FK→websites,**unique** | N |
| online | boolean? | | S |
| http_status | int? | | S |
| checked_at | timestamp? | "最近检测时间" | S |
| ssl_expires_at | timestamp? | | S |
| ssl_days_remaining | int? | | S |
| cdn / hosting | text? | | S |
| technologies | jsonb | 名称列表 | S |
| summary_hash | text | 本地摘要序列化 hash(变化检测) | N |
| last_sync_at | timestamp | 导航站上次同步时间 | N |

### website_metrics(长期统计,冻结版 §7 语义)

| 字段 | 类型 | 说明 | 来源 |
|---|---|---|---|
| id | text | PK | N |
| website_id | text | FK→websites,**unique** | N |
| sync_count | int | 同步次数 | N |
| event_count | int | 变化信号计数 | N |
| task_count | int | 挂载任务数 | N |
| month_clicks | int | 月点击计数 | N |

### website_changes(结构化变化事件;Schema V1.0 冻结版 §2.13)

**四层语义(冻结)**:website_snapshots = 历史事实摘要快照(永久保留);website_status = 当前状态聚合(单行);website_metrics = 长期累计统计;website_changes = 结构化变化事件(每站保留最近 20 条滑动)。四表职责不重叠、**不合并**。

| 字段 | 类型 | 说明 | 来源 |
|---|---|---|---|
| id | text | PK | N |
| website_id | text | FK→websites | N |
| change_type | text | 如 `online/http_status/ssl/tech/...` | S 派生 |
| severity | text | `info/warning/critical` | S 派生 |
| detected_at | timestamp | 事件时间 | S 派生 |
| delta | jsonb | 变化明细 `{previous,current}` | S 派生 |
| created_at | timestamp | 索引(website_id, created_at DESC) | N |

### recommendations(稳定推荐定义层,非实时搜索缓存;冻结版 §15 持久化可审计)

> 仅由编辑/人工决策写入:编辑精选、Task×Website 长期推荐关系、人工排序覆盖、需审计的固定推荐依据、推荐规则的重要人工配置。用户实时搜索的排序结果**默认不写此表**(实时计算在内存完成,§8.6)。

| 字段 | 类型 | 说明 | 来源 |
|---|---|---|---|
| id | text | PK | N |
| website_id | text | FK→websites | N |
| task_id | text? | FK→tasks(挂载的 Task) | N |
| context | text | `task_page/search/detail/home`(仅编辑决策写入时记录) | N |
| score | float | 五因子加权分(编辑决策时计算;实时搜索不写入) | N |
| confidence | text | `high/medium/low`(映射 §7.4) | N |
| source | text | `rule/manual` | N |
| status | text | `active/superseded` | N |
| | | 索引(website_id, task_id, context) | |

### recommendation_evidence(可追溯,冻结版 §5/§15 硬要求)

| 字段 | 类型 | 说明 |
|---|---|---|
| id | text | PK |
| recommendation_id | text | FK→recommendations,Cascade |
| evidence_type | text | enum: `task_match/constraint_match/quality/status_signal/editorial`(冻结版五因子) |
| factor_name | text | 具体因子(如 `task:制作PPT`、`tag:free`) |
| score | float | 分项得分 |
| basis | jsonb | 依据明细(匹配的 alias/命中的 tag/质量维度/状态字段/精选条目) |
| generated_by | text | `rule/manual` |

### curated_picks(编辑精选 / 今日推荐)

| 字段 | 类型 | 说明 |
|---|---|---|
| id | text | PK |
| website_id | text | FK→websites |
| task_id | text? | 可选挂载 |
| reason | text | 编辑理由(推荐理由素材) |
| sort_order | int | |
| status | text | `draft/published/archived` |
| published_at | timestamp? | |

### submissions(用户提交)

| 字段 | 类型 | 说明 |
|---|---|---|
| id | text | PK |
| domain | text | 用户提交原始域名(展示/审核参考) |
| normalized_domain | text | unique(去重;www/裸域归并) |
| note | text | 提交者说明 |
| ip / user_agent | text | 防刷记录(不展示) |
| status | text | `pending/approved/rejected/duplicate` |
| reviewed_by / reviewed_at / review_note | text?/timestamp?/text? | |

### reports(数据纠错)

| 字段 | 类型 | 说明 |
|---|---|---|
| id | text | PK |
| website_id | text | FK→websites |
| field | text | 纠错字段(如 title/online/category) |
| current_value / expected_value | jsonb | |
| note | text | 用户说明 |
| status | text | `pending/accepted/rejected/resolved` |
| resolved_value | jsonb? | 接受后的修正值 |

### user_events(匿名行为,冻结版 §13)

| 字段 | 类型 | 说明 |
|---|---|---|
| id | text | PK |
| visitor_id | text | 匿名 cookie,无注册 |
| event_type | text | `visit/reason_expand/submit/search_click`(收藏为纯前端本地数据,不产生服务端事件,§10.1) |
| website_id / task_id | text? | FK |
| payload | jsonb? | |
| created_at | timestamp | 索引(visitor_id, event_type)、(event_type, created_at) |

### search_logs

| 字段 | 类型 | 说明 |
|---|---|---|
| id | text | PK |
| query / query_normalized | text | |
| task_id | text? | 识别结果(Top-1) |
| constraints | jsonb? | 提取的约束 |
| result_count | int | |
| clicked_website_id | text? | 点击回流 |
| visitor_id | text? | |
| created_at | timestamp | 索引(query_normalized, created_at) |

## 6.2 导航站辅助表

### sync_queue

| 字段 | 类型 | 说明 |
|---|---|---|
| id | text | PK |
| domain | text | 同步对象原始 Host(展示/日志) |
| normalized_domain | text | **unique 实体键**(www 折叠,与 websites 同规则) |
| job_type | text | CHECK: `sync`(P0 唯一;**UNIQUE(normalized_domain, job_type)** 幂等) |
| status | text | `pending/processing/completed/retrying/failed/skipped` |
| locked_at | timestamp? | worker 领取时间(锁;超时恢复依据,§4.2) |
| locked_by | text? | 领取 worker 标识(单实例 = 进程 ID;未来多实例 = 实例名) |
| attempts | int | 默认 0;失败/超时累计,自增后用更新后值判定(§4.2) |
| max_attempts | int | 默认 5;attempts >= max_attempts → failed(第 5 次失败即 failed,无第 6 次) |
| next_retry_at | timestamp? | 索引(status, next_retry_at);到期后重新可领取 |
| last_error / last_http_status | text?/int? | |
| summary_hash | text? | 去重参考 |
| updated_at | timestamp | 每次状态变更刷新;`processing` 超过锁持有期 → 超时恢复(§4.2) |

### sync_logs

| 字段 | 类型 | 说明 |
|---|---|---|
| id | text | PK |
| domain | text | 原始 Host(记录) |
| normalized_domain | text | 实体关联键(关联查询用;不做去重键) |
| action | text | `analyze/report/diff` |
| status / http_status / error | text?/int?/text? | |
| duration_ms / payload_size | int | |
| created_at | timestamp | 索引(normalized_domain, created_at) |

### settings(权重与阈值配置)

| 字段 | 类型 | 说明 |
|---|---|---|
| key | text | **unique**(如 `ranking.weights`、`quality.weights`、`task.index_thresholds`、`sync_queue.max_attempts`(默认 5)) |
| value | jsonb | |
| updated_at | timestamp | |

### admin_users(最小登录,冻结版"不建复杂角色体系")

| 字段 | 类型 | 说明 |
|---|---|---|
| id | text | PK |
| email | text | unique |
| password_hash | text | scrypt |
| name | text? | |
| created_at | timestamp | |

## 6.3 索引与约束要点

- 唯一约束:websites.normalized_domain、categories.slug、tags.slug、tasks.slug、task_aliases.alias_normalized、task_related_tags(task_id,tag_id)、website_tags(website_id,tag_id)、website_tasks(website_id,task_id)、website_relations(from,to,type)、website_status.website_id、sync_queue(normalized_domain, job_type)、submissions.normalized_domain、settings.key、admin_users.email。
- 关键索引:user_events(visitor_id,event_type)、search_logs(query_normalized)、website_snapshots(website_id,type,created_at)、sync_queue(status,next_retry_at)、sync_queue(status,locked_at)(超时恢复扫描)、recommendations(website_id,task_id,context)。
- JSON 字段清单:websites 无;website_status.technologies、website_snapshots.data、website_changes.delta、recommendation_evidence.basis、reports.current_value/expected_value、user_events.payload、search_logs.constraints、settings.value、submissions.note(纯文本)。

## 6.4 数据来源汇总(审计要求)

| 数据 | 归属 |
|---|---|
| 域名基础信息/状态/SSL/技术栈/ASN·geo 摘要/变化信号 | SiteIntel(S),导航站只存业务摘要(§4.4),不存全量 |
| 主分类、任务、标签、关系、推荐、精选、提交、纠错、行为、搜索日志 | 导航站(N),SiteIntel 无此概念 |
| 派生层(质量分、hash、索引位、指标) | 导航站(N) |

---

# 7. 搜索与 Query 解析架构

## 7.1 管线(P0 无 LLM,冻结版 §4)

```text
query
  → normalize    小写、全角转半角、去首尾空白
  → 停用词过滤   词典: 的/了/哪个/最好/网站/工具/有什么/推荐 等(不含"免费/AI/在线"——它们是约束词)
  → token 化     按空白 + 连续中文串切分(无分词器,不做新词发现)
  → task 匹配    alias 词典包含匹配(§7.2)
  → constraint   约束词典 → 标签(§7.3)
  → 候选         命中 Task: website_tasks 候选集; 未命中: ILIKE 兜底
  → 过滤         按约束标签过滤
  → 排序         §8 五因子
  → 记录         search_logs(识别结果/结果数/点击)
```

## 7.2 Task Matcher(alias 词典)

- **词典构建**:task_aliases 表(alias_normalized 全局唯一)+ tasks.name + slug,启动时载入内存(任务量 30-50,规模极小);后台编辑保存时热更新。
- **匹配规则**(按优先级计分):
  1. 全等命中(归一化后 query == alias)得分最高;
  2. 前缀/包含命中(query 包含 alias,或 alias 包含 query 核心词);
  3. 多 Token 联合命中(query 的多 token 分别落在同一 task 的不同 alias);
  4. 命中数相同 → 取候选网站数更多的 task(防薄任务抢位)。
- **Top-1 输出**:`{ task_id, confidence }`;命中率验收:种子 Task 覆盖范围内 Top-1 ≥85%(固定 100 条人工标注测试集,冻结版 §19)。

## 7.3 Constraint Mapper

- **词典**:constraint 词 → tag 映射,存 settings(`constraint.dictionary`):
  `免费→tag(name="免费", tag_type=constraint)`、`AI→tag(name="AI", tag_type=feature)`(产品语义:技术形态/功能特征,属四种固定类型)、`在线→tag(name="在线", tag_type=feature)`、`中文→tag(name="中文", tag_type=attribute)`、`无需注册→tag(name="免注册", tag_type=attribute)` 等。
- **tag_type 固定四枚举**(attribute/feature/constraint/scenario,冻结版 §14):禁止新增任何第五种类型。若后台需要按"商业模式 / 技术形态 / 使用平台 / 价格方式"等维度分组展示,只能使用 `tags.group` 可选字段 —— 它只是管理分组维度,**绝不替代 tag_type**,不参与搜索/匹配/过滤语义。
- **映射值**落到 tags(四种 tag_type 之一),与 website_tags 过滤共用同一套标签体系(冻结版"约束→标签"硬规则)。
- 未识别的 token 不进 constraint,不影响排序。

## 7.4 无匹配处理

1. Task 未命中 → 兜底搜索:`ILIKE %query%` 匹配 websites.name/title/description + tasks.name(中文包含即命中,PostgreSQL 无需扩展);
2. 兜底仍无结果 → 空态:展示热门任务入口(编辑精选替代,不展示"伪热门");
3. 无匹配 query 全部记入 search_logs,作为 Task 词典新增的候选证据(冻结版 §23.2 实体优先:不因一次搜索就建 Task)。

## 7.5 多 Task 冲突

- 按 §7.2 规则 4 决出 Top-1;次选 Task 在结果页顶部以"相关任务"链接呈现(不混入主结果集)。
- 冲突样本沉淀到 search_logs,编辑后台"搜索日志分析"(P1)迭代词典。

## 7.6 验收(冻结版 §19 落实)

- 固定 100 条人工标注 query 测试集(vitest 用例,随仓库入库);
- 只统计种子 Task 覆盖范围内、理论上应能识别的 query;
- 指标:`Task Top-1 命中率 ≥85%`;Constraint 单独统计(召回率 ≥80% 为内控目标,不设产品验收线)。

---

# 8. 推荐与排序架构

## 8.1 五因子(冻结版 §4 P0 排序,零黑盒)

| 因子 | 含义 | 权重(默认) | 可配置 |
|---|---|---|---|
| task_match | 任务匹配度(website_tasks.sort_weight + match_basis 质量) | 0.30 | ✅ |
| constraint_match | 约束/特征匹配度(命中的 constraint 标签数 / 应命中数) | 0.20 | ✅ |
| quality | 数据质量评分(§8.3) | 0.20 | ✅ |
| status_signal | 可验证状态信号(§8.4) | 0.10 | ✅ |
| editorial | 编辑精选/人工判断(§8.5) | 0.20 | ✅ |

权重存 settings(`ranking.weights`),后台可调,改动即时生效(重排时生效)。

## 8.2 排序公式

```text
score = w_task   × task_match
      + w_const  × constraint_match
      + w_quality× quality
      + w_status × status_signal
      + w_editor × editorial

各分项归一化到 0-100;满分 100。
task_match:     sort_weight(默认 50) + 人工加分;无 match_basis 扣 10 分(完整性治理)
constraint_match: 应命中 = query 提取的 constraint 数; 命中率 = 命中数/应命中数 × 100
quality:        data_quality_score(§8.3, 0-100)
status_signal:  online=100 / offline=0; 再按 checked_at 新鲜度衰减(>7 天每 7 天 -10, 下限 40);
                ssl_days_remaining ≤14 → 额外 -20(证书即将到期可验证信号)
editorial:      curated_picks 命中该网站 → 100; 否则 0
```

## 8.3 数据质量评分(冻结版 §6 六维,内部指标非"网站好坏")

| 维度 | 计分 | 权重(默认) |
|---|---|---|
| 基础信息完整度 | name/description/分类齐全 | 0.20 |
| Category/Task 完整度 | 有主分类 + ≥1 任务 | 0.20 |
| 推荐依据完整度 | website_tasks.match_basis 覆盖率 | 0.20 |
| 当前可访问状态 | online 有值 | 0.15 |
| 数据新鲜度 | last_sync_at ≤7 天 | 0.15 |
| 人工核验状态 | 编辑核验过(标记字段) | 0.10 |

权重存 settings(`quality.weights`),后台可配;P0 规则评分,无机器学习。

## 8.4 可解释推荐理由(冻结版 §7 六维前五,模板生成非 LLM)

| evidence_type | 文案模板 | 数据来源 |
|---|---|---|
| task_match | "与你搜索的'制作PPT'任务匹配" | website_tasks |
| constraint_match | "支持免费使用、无需注册(已按你的筛选条件匹配)" | tags + website_tags |
| quality | "信息完整度高,核心功能已验证" | data_quality_score ≥80 时展示 |
| status_signal | "最近检测正常访问(检测于 X)" / "SSL 证书将于 X 到期" | website_status |
| editorial | "编辑精选推荐" | curated_picks |

- 持久化规则分两层(详见 §8.6):**实时搜索执行层**仅内存生成、不落库;**稳定推荐定义层**(编辑决策/人工覆盖/固定 Task×Website 推荐关系)才写 recommendations + recommendation_evidence(evidence_type/factor_name/score/basis),满足"为什么推荐这个"可追溯(冻结版 §5/§15);
- 前端可展开信息卡片(冻结版 §7.3,P0)。

## 8.5 人工覆盖机制

1. **website_tasks.override_score**:编辑对"某网站×某任务"固定评分,排序时直接替换计算分(置 override_score 后该因子以 override 为准);
2. **curated_picks**:精选命中 → editorial=100;
3. **relations 人工标注**:替代关系人工判定(冻结版 §5,P0 少量精选);
4. 覆盖都带 `source=manual` 入 evidence,后台可见来源;
5. 所有人工决策(创建/修改推荐、排序覆盖、明确建立稳定 Task×Website 推荐关系)落库 recommendations + recommendation_evidence —— 这是稳定推荐定义层允许持久化的全部场景(§8.6)。

## 8.6 两层职责:稳定推荐定义层 vs 实时搜索执行层

| 层 | 使用对象 | 是否写 recommendations / recommendation_evidence | 内容与时机 |
|---|---|---|---|
| **稳定推荐定义层** | recommendations + recommendation_evidence 表 | ✅ 是 | 编辑精选、Task×Website 长期推荐关系、人工排序覆盖、需审计的固定推荐依据、推荐规则的重要人工配置。数据可解释 / 可追溯 / 可审核(冻结版 §15)。写入时机:编辑在后台创建或修改推荐、人工覆盖排序、明确建立稳定推荐关系、需长期审计的推荐决策(§8.5)。 |
| **实时搜索执行层** | 用户每次搜索的内存计算 | ❌ 否(默认) | Query → Task+Constraint → 候选 → 实时计算 → 实时排序 → 实时生成推荐理由,全部在内存完成。结果仅返回响应,不落库。 |

硬规则:**任何一次用户搜索 / 任务页访问,都不得新增 recommendations 行**。否则会形成"用户搜索 → 实时推荐 → 大量重复记录 → 数据库污染",并使审计表失去语义。

---

# 9. 管理后台架构(最小版)

## 9.1 页面树

```text
/admin                       总览(待审提交数/待处理纠错数/同步队列摘要/索引状态)
├── /admin/websites          网站列表(搜索/筛选 status,is_indexed) + 新建/编辑(表单)
│     └── /admin/websites/[id]  详情(字段/标签/任务/关系/状态/同步信息/纠错记录)
├── /admin/categories        分类树(父/子,sort_order,is_indexed)
├── /admin/tasks             任务列表(候选数/索引状态) + 编辑(名称/别名 task_aliases/说明/关联标签 task_related_tags)
├── /admin/tags              标签管理(tag_type 过滤,合并/改名)
├── /admin/submissions       用户提交审核队列(批准→建 website 草稿/拒绝/标记重复)
├── /admin/reports           数据纠错队列(接受→回写字段/拒绝/解决)
├── /admin/recommendations   推荐依据维护(website×task 匹配依据、override_score)
├── /admin/curated-picks     编辑精选管理(草稿/发布/归档,首页与任务页展示位)
├── /admin/settings          权重配置(ranking/quality/task 阈值/constraint 词典)
├── /admin/sync              同步队列监控(状态/重试/手动触发/日志)
└── /admin/seo               索引状态检查(低于阈值自动降级清单)
```

## 9.2 页面要素(统一)

| 要素 | 说明 |
|---|---|
| 数据来源 | 全部导航站库;无 SiteIntel 直连 |
| 操作 | 列表(分页/筛选)→ 详情 → 编辑表单 → 保存(即时生效,任务页 ISR revalidate) |
| 状态流转 | websites: draft→active→archived;submissions: pending→approved/rejected/duplicate;reports: pending→accepted/rejected/resolved;curated: draft→published→archived;tasks: draft→active(阈值达标自动 is_indexed) |
| 权限 | ADMIN_EMAILS 白名单 + 登录 session;单角色,无 RBAC(冻结版 §12 原则) |

## 9.3 后台不做的(冻结版明确否定)

复杂角色体系(站长/编辑/审核员分立)、工作流引擎、站内信、审计日志系统(仅 sync_logs 保留)。

---

# 10. API 契约

> 统一约定:JSON 响应;错误码 `400 参数错 / 401 未登录 / 403 越权 / 404 不存在 / 429 限流 / 500 服务端`;错误体 `{ error: string, details? }`;分页 `?page=1&page_size=20(≤100)` → `{ items, page, page_size, total }`;公开只读端点匿名可用;提交类端点内存限流(同 SiteIntel 实现)。

## 10.1 Public API

### GET /api/search

```
?q=免费做PPT的AI网站&page=1&page_size=20
→ 200 { query, task: { slug, name } | null, constraints: ["free","ai"],
        items: [ { website: { domain, name, slug, description }, score, confidence,
                   reasons: [ { type, text } ], status_signal: { online, checked_at } } ],
        page, page_size, total }
→ 400 q 为空/超长(≤200 字符)
→ 429 超限(内存滑动窗口 60/min/IP)
缓存: 响应不缓存(个性化/实时性);高频热词可在应用层 30s 内存缓存
持久化: 排序与推荐理由实时内存计算,不写 recommendations/recommendation_evidence(§8.6)
```

### GET /api/tasks/:slug

```
→ 200 { task: { slug, name, description, aliases_count /* 来自 task_aliases */ }, websites: [ 同 search items 结构 ],
        total }
→ 404 任务不存在或未到索引阈值(低于阈值不返回列表,只返回 404)
缓存: ISR 60s;编辑发布时 revalidate 指定 slug
```

### GET /api/websites/:domain

```
→ 200 { website: { domain, name, title, description, summary_text, category, tags, tasks,
                   relations: { similar[], alternative[], related[] }, status_signal,
                   quality_score, reasons[] } }
→ 404 网站不存在
缓存: ISR 60s
```

### GET /api/categories/:slug

```
→ 200 { category: { slug, name, description, children[], websites: [...] } }
→ 404 不存在
缓存: ISR 60s
```

### POST /api/submissions

```
body { domain, note? } → 201 { id, status: "pending" }
→ 400 域名非法 / 422 重复提交(unique domain 已存在且未拒绝)
→ 429 限流(3/min/IP)
```

### POST /api/reports

```
body { website_id 或 domain, field, current_value?, expected_value, note? }
→ 201 { id, status: "pending" }
→ 404 网站不存在 / 400 字段非法 / 429 限流
```

### POST /api/events

```
body { event_type, website_id?, task_id?, payload? }
(visitor_id 由服务端从 cookie 生成/读取,不接受客户端传入)
→ 201 { ok: true }
→ 400 event_type 非法(白名单) / 429 限流
```

### 本地收藏(冻结版 §13,无服务端端点)

```
P0 收藏 = 纯前端本地数据(localStorage 或 IndexedDB,由前端决定)
- 不写入服务器数据库, 不提供任何服务端端点
- visitor_id 仅用于匿名、非敏感行为统计(点击/搜索/匿名 usage event/统计聚合),
  禁止参与收藏; 禁止在 P0 形成"匿名用户收藏数据库"
- P1 账号体系后提供云端收藏 + 本地收藏迁移(冻结版 §13)
```

## 10.2 Admin API(全部需登录 session + ADMIN_EMAILS)

| 端点 | 操作 |
|---|---|
| POST /api/admin/websites | 新建(可带 category/tags/tasks 关联) |
| PATCH /api/admin/websites/:id | 更新字段/状态/索引开关;触发任务页 revalidate |
| POST /api/admin/websites/:id/sync | 手动触发同步入队 |
| GET/POST/PATCH /api/admin/categories | 列表/新建/更新(slug/父子/排序/索引) |
| GET/POST/PATCH /api/admin/tasks | 列表/新建/更新(别名与关联标签经 task_aliases / task_related_tags 整体提交) |
| GET/POST/PATCH /api/admin/tags | 列表/新建/更新(tag_type 变更需校验) |
| GET /api/admin/submissions?status=pending | 审核队列 |
| POST /api/admin/submissions/:id | 批准(建草稿)/拒绝/重复(带 review_note) |
| GET /api/admin/reports?status=pending | 纠错队列 |
| POST /api/admin/reports/:id | 接受(回写字段)/拒绝/解决 |
| GET/PATCH /api/admin/recommendations | 推荐依据维护(website×task 的 match_basis/sort_weight/override) |
| GET/POST/PATCH /api/admin/curated-picks | 精选管理(排序/发布/归档) |
| GET/PATCH /api/admin/settings | 权重与阈值(白名单 key) |
| GET /api/admin/sync | 队列状态;POST /api/admin/sync/reset/:id 重置失败项 |

## 10.3 缓存策略汇总

| 层 | 策略 |
|---|---|
| 页面 | 任务页/分类页 ISR(revalidate 60s);编辑发布时按 slug revalidate;参数页(排序/筛选)noindex 且不缓存 |
| API | /api/tasks/*、/api/categories/* ISR 60s;搜索不缓存(或 30s 内存热词缓存);提交类不缓存 |
| 数据库 | 常规索引 + 查询优化;无独立缓存层(数据量小) |
| 上游 | 同步摘要落库,页面不实时调 SiteIntel(离线优先) |

---

# 11. 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| 前端/后端 | Next.js(App Router)+ React + TypeScript strict | 与 SiteIntel 同栈(16.x),模式已验证,Claude Code 开发稳定 |
| 样式 | Tailwind CSS 4 | 与 SiteIntel 一致 |
| 数据库 | PostgreSQL 14 | 154 服务器现成实例;独立库/独立用户 |
| ORM | Prisma 6 + migrations | 与 SiteIntel 一致;迁移工作流已建立 |
| 缓存 | 无独立组件(ISR + revalidate) | P0 数据量小(数百网站),Redis 徒增运维 |
| 队列 | 数据库表状态机(sync_queue) | 单实例、低频、可断点续跑;不需要消息中间件 |
| 定时任务 | instrumentation.ts setInterval | 与 SiteIntel 同款,生产已验证(注意 NEXT_PHASE 构建守卫) |
| 搜索 | 应用层词典 + PostgreSQL ILIKE | P0 规则引擎,无 ES;规模大了再评估 pg_trgm/zhparser(P2) |
| 限流 | 内存滑动窗口(复用 SiteIntel 实现思路) | 单实例足够 |
| 登录 | 邮箱+scrypt+JWT session cookie(自研,复用 SiteIntel 模式) | 仅后台管理员;P0 不引第三方 Auth |
| 部署 | 154 服务器 + systemd + nginx 反代 + Cloudflare | 与 SiteIntel 同工作流(pnpm build → systemd restart);端口 3010(3003 被占) |

**明确的"不引入"清单**:Redis、Elasticsearch、BullMQ、Docker/K8s、第三方 Auth 服务、GraphQL、微服务。理由:P0 规模下全部是复杂度而非价值;与 SiteIntel 运维心智保持一致。

---

# 12. 部署架构

```text
154.204.176.66 (与 SiteIntel 同机, 独立 systemd 服务)
├── systemd: navigation (端口 127.0.0.1:3010, --hostname 127.0.0.1 仅本机可连)
├── nginx: 域名 vhost → 反代 127.0.0.1:3010
├── Cloudflare: 域名代理 + HTTPS
├── PostgreSQL: 独立库(如 nav_disc)+ 独立用户; 与 siteintel 库隔离
└── 环境变量(.env, 不入库):
    DATABASE_URL / SESSION_SECRET / ADMIN_EMAILS
    SITEINTEL_API_URL=https://siteintel.cc/api/v1 / SITEINTEL_API_KEY=si_...(navigation-sync 专用)
```

- 构建部署:SSH 上传 → `pnpm install && pnpm build && systemctl restart navigation`(与 SiteIntel 相同流程,含 nvm PATH 注意事项);
- 域名:待定(未决项 §14);
- 定时任务随应用进程(单实例部署,无多实例竞态问题;若未来多实例,DB 队列需加乐观锁)。

---

# 13. P0 开发模块划分(Phase 1,对应冻结版 §12)

| 模块 | 内容 | 依赖 |
|---|---|---|
| M1 工程骨架 | Next.js + Prisma + migrations + 布局/主题 + instrumentation | — |
| M2 数据模型与迁移 | §6 全部表 + seed(30-50 种子任务/分类/标签) | M1 |
| M3 同步服务 | sync_queue 状态机 + 节流 + 拉取/裁剪/写入 + hash diff + 后台同步页 | M2、SiteIntel Key |
| M4 搜索与解析 | parser/task-matcher/constraint-mapper/search-service + 100 条测试集 | M2 |
| M5 推荐与排序 | ranker/reasons/quality-scorer;落库仅限稳定推荐定义层(编辑决策/人工覆盖),实时搜索不落库(§8.6) | M2 |
| M6 用户端页面 | 首页(精选/任务入口)/任务页/分类页/详情页(四层信息架构+推荐卡片+状态信号)/搜索页/收藏 | M4 M5 |
| M7 提交与纠错 | 用户提交/纠错表单 + 队列处理 | M6 |
| M8 管理后台 | §9 全部页面与 Admin API | M2 M3 M5 |
| M9 SEO 基础设施 | sitemap/robots/Schema.org/canonical/noindex/任务页阈值+自动降级 | M6 |
| M10 验收 | §19 Task 识别测试集、§21 P0 验收清单、§20 指标埋点(user_events/search_logs;收藏率指标依赖 P1 账号体系,本地收藏阶段服务端无收藏数据) | 全部 |

依赖链:M1 → M2 → {M3, M4} → M5 → M6 → {M7, M8, M9} → M10。M3 可与 M4 并行;M6 详情页可先于推荐完整度用占位分。

---

# 14. 风险与未决问题

| # | 问题 | 影响 | 处理 |
|---|---|---|---|
| 1 | **域名未定** | 部署/SEO/canonical 依赖 | 架构冻结后立即决策(建议与 SiteIntel 同后缀:find.siteintel.cc?或独立域,待用户定) |
| 2 | SiteIntel 缺口 1(analyze 30/h) | 冷启动慢(500 域名 ≈17h) | 已规划分批;请 SiteIntel 排期放开硬顶(小票) |
| 3 | SiteIntel 缺口 2(无增量 events API) | P1 更新提醒依赖 | P0 用摘要 diff 降级;SiteIntel 排期新增 /api/v1/events |
| 4 | 服务器 PG 扩展未验证 | ILIKE 兜底无扩展依赖 ✅;pg_trgm 仅 P2 可选 | 架构不依赖,标注待验证 |
| 5 | 中文搜索无分词器 | 词典驱动足够(Top-1 ≥85% 靠 alias 词典);长句泛匹配靠 ILIKE | 验收测试集先行;不达标再评估 |
| 6 | 编辑人力预算 | 30-50 任务 × 内容生产是最长路径 | Phase 0 先跑通 3-5 个任务测算单任务成本(冻结版 §10) |
| 7 | ISR 与"编辑发布即生效" | revalidate 需要触发点 | M6 实现 revalidateTag/按 slug revalidate,后台保存时调用 |
| 8 | 本地收藏的 P1 迁移 | localStorage/IndexedDB → 账号云端收藏 | 冻结版 §13 已定:账号 P1 后迁移(合并策略以收藏时间/去重);P0 服务端不存收藏,visitor_id 不参与(§10.1) |
| 9 | 多实例部署竞态(未来) | sync_queue 双实例并发取任务 | 单实例部署规避;未来加乐观锁(updated_at CAS) |
| 10 | SiteIntel 鉴权变更风险 | 同步断供 | 同步失败不影响页面(本地缓存兜底),告警到 sync_logs 日志 |

---

# 15. 内部一致性检查(完成即修正)

逐项对照冻结版执行:

1. **实体覆盖**:§6 表清单 = 冻结版 §15 全部 15 组实体 ✅(websites/categories/tags/website_tags/tasks/website_tasks/website_relations/website_snapshots/website_status/website_metrics/recommendations/recommendation_evidence/curated_picks/submissions/reports/user_events/search_logs)+ 导航站辅助表(sync_queue/sync_logs/settings/admin_users)✅
2. **三表语义**:snapshots=导航站侧同步后的网站事实摘要快照(非 SiteIntel 全量镜像,§6.1)、status=当前聚合状态、metrics=长期统计指标,三者语义清晰且不重叠 ✅(冻结版 §7,B1)
3. **搜索链路**:Query→Task→Constraint→候选→排序,引用同一 alias 词典与标签体系,无第三套映射表 ✅(冻结版 §3.4/§8)
4. **推荐五因子** = 冻结版 §4 P0 排序五因子(名称逐一对应)✅;evidence_type 五枚举对应五因子 ✅
5. **状态信号口径**:全文只有"最近检测时间/可访问状态/近期变化/SSL 到期",无"活跃度"表述 ✅(冻结版 §23.3)
6. **管理端九项** = 冻结版 §12 管理端九项逐一对应 ✅
7. **边界硬规则**:同步只经 v1 API + 专用 Key;明确禁止直连 SiteIntel 库;摘要裁剪清单 §4.4 明确不存全量 ✅(冻结版 §11)
8. **P0/Phase 同步**:M1-M10 全部落在 Phase 1;P1 能力(账号/更新提醒/相关关系/对比页/专题榜单)不进入 P0 模块 ✅(冻结版 §16/§17)
9. **收藏与行为分离**:收藏为纯前端本地数据(localStorage/IndexedDB),服务端无收藏端点;visitor_id 仅匿名、非敏感行为统计,不参与收藏 ✅(冻结版 §13,B3)
10. **验收对齐**:§19 Task 识别测试集口径、§21 P0 验收清单、§20 指标全部有落点 ✅
11. **tag_type 四枚举固定**:全文仅 attribute/feature/constraint/scenario;tags.group 仅为可选管理分组维度,不参与匹配语义 ✅(冻结版 §14,A1)
12. **实时搜索不污染**:recommendations / recommendation_evidence 仅稳定推荐定义层(编辑决策/人工覆盖/固定推荐关系)写入;搜索与任务页实时结果默认不落库 ✅(冻结版 §15,A2)
13. **sync_queue 防重**:原子领取(CAS 条件更新)+ locked_at/locked_by 锁 + 超时恢复(含 systemd 重启)+ 重试退避 + 幂等 + 断点续跑 ✅(B2)

**一致性检查中发现并修正的问题**:
- 初稿把 `website_status` 设计为含全量 technologies JSON,检查发现与"只存摘要"冲突 → 已收敛为"技术名称列表",明细归 SiteIntel(§4.4);
- 初稿 API 把 `POST /api/events` 设计为接收 visitor_id → 已改为服务端生成/读取 cookie(防伪造,§10.1);
- 初稿将任务页阈值复核放 M9,检查发现阈值自动降级是 SEO 基础能力 → 已并入 M9 且标注为"低于阈值自动 noindex"(冻结版 §14.2 要求);
- 本轮外部审计修订 A1/A2/B1/B2/B3 全部回写正文(详见 #17 修订记录),检查项 2/9 更新,新增检查项 11/12/13。

---

# 16. 结论

1. **架构成立**:P0 闭环(需求→Task+Constraint→候选→可解释推荐→比较→点击→收藏)+ 最小管理后台 + 数据纠错闭环,全部由本架构覆盖;无新技术风险点,全部沿用 SiteIntel 已验证模式。
2. **上游依赖可控**:两个缺口均不阻塞 P0,已规划降级路径;SiteIntel 侧两处小改动建议排期。
3. **可直接进入下一阶段**:数据库 Schema 与 API 契约冻结,以本报告 §6 与 §10 为底稿做字段级确认。
4. **未决事项先行**:域名决策(风险 #1)与服务器 PG 权限确认原应在 Schema 冻结前完成;Schema V1.0 已于 2026-08-17 正式冻结(验收 PASS),未决项仅记录不阻塞(见冻结验收记录"已知未决项")。
5. **修订状态**:V1.0-rev2(2026-08-17)已与 Schema V1.0 冻结版完成字段级基线同步(#19),**暂不冻结**,等 API 契约阶段复核。

---

# 17. V1.0 候选版修订记录

> 2026-08-17 外部技术架构审计结论:架构整体成立,不需要推翻、不需要重新设计。本轮只修复 5 个已明确的问题(A1/A2/B1/B2/B3),全部直接回写正文,**暂不冻结**。
> 【历史记录】本节为 2026-08-17 外部审计修订的原始记录,内容不再改写;其中涉及 Schema 的字段描述(如 domain unique、month_favorites、aliases 等)以正文为准——正文已于 V1.0-rev2 与 Schema V1.0 冻结版完成基线同步(#19)。

## A1:Tag 类型与分组语义冲突

- **§7.3 Constraint Mapper**:词典示例从"免费→tag:商业模式·免费 / AI→tag:技术形态·AI"改为四枚举写法(`免费→tag(name="免费", tag_type=constraint)`、`AI→tag(name="AI", tag_type=feature)` 等);明确 tag_type 固定四枚举 attribute/feature/constraint/scenario,禁止新增(business_model/technology/pricing/platform 均不允许);明确 tags.group 仅为可选管理分组维度,不替代 tag_type、不参与匹配语义。
- **§6.1 tags 表**:新增可选字段 `group`(如 price/tech/platform,仅后台分组展示)。
- **§5.1 数据流**:constraint-mapper 示例同步改为四枚举写法。
- 未新增任何 tag_type,未改变冻结版 §14 语义。

## A2:recommendations 被搜索请求污染

- **§5.1 搜索主链路**:reason-builder 由"生成证据文案 + 落库 recommendations/recommendation_evidence"改为"实时生成推荐理由(内存计算,默认不落库)"。
- **§5.2 编辑侧闭环**:新增"编辑推荐决策(创建/修改/人工覆盖)→ 落库"路径。
- **§6.1 recommendations**:定义改为"稳定推荐定义层,非实时搜索缓存";字段说明(task_id/context/score)同步更新,注明实时搜索不写入。
- **§8.4**:持久化规则改为两层表述;新增 **§8.6 两层职责表**(稳定推荐定义层 ✅ 落库 / 实时搜索执行层 ❌ 默认不落库)+ 硬规则"任何一次用户搜索/任务页访问不得新增 recommendations 行"。
- **§8.5 人工覆盖**:明确允许持久化的全部场景(编辑创建/修改推荐、人工覆盖排序、稳定 Task×Website 推荐关系、需长期审计的推荐决策)。
- **§10.1 GET /api/search**:注明实时结果不写 recommendations/recommendation_evidence。
- **§13 M5**:模块描述同步更新。
- 消除"每次搜索自动落库 recommendation"的所有歧义。

## B1:website_snapshots 数据语义

- **§6.1 website_snapshots**:正式定义为"导航站侧同步后的网站事实摘要快照",新增语义定义段:不是 SiteIntel 全量原始检测数据的镜像;evidence.rawData / 完整检测结果 / 原始 evidence 仍属 SiteIntel;列出摘要字段范围(checked_at/online/status/http_status/ip·asn·geo 单值摘要/technologies 名称/ssl 有效期/cdn/hosting/seo-health 摘要/summary_hash)。
- **§4.4 裁剪清单**:新增 asn_org/country 单值摘要行;明确不存清单扩展(完整检测结果、原始 evidence、IP 明细列表),强调"绝不建立 SiteIntel 数据库镜像"。
- **§4.1 步骤 6**:措辞由"原始快照写 website_snapshots"改为"业务摘要快照"。
- **§6.4 汇总表 / §15 检查项 2**:三表语义(snapshots/status/metrics)复核为清晰不重叠。

## B2:sync_queue 任务领取与 Worker Lock

- **§4.2**:状态机由 pending→fetching→analyzing→done 重构为 `pending → processing → completed / retrying / failed / skipped`;新增"任务领取与 Worker Lock"小节(8 点):
  - 原子领取 = 单条条件更新(CAS),给出 SQL 语义与影响行数判定;禁止"先 SELECT 再 UPDATE"两段式;
  - 同任务防重(processing 不满足领取条件);
  - 超时恢复:locked_at 超过 10 分钟(锁持有期)→ retrying,覆盖 systemd 重启断点续跑;
  - 失败/重试/退避:attempts+1、指数退避 min(2^attempts×60s, 4h)、上限 5 次 → failed 人工处置;
  - 幂等(domain unique + summary_hash 零写);
  - 单实例现状 + 未来多实例直接复用 locked_by/原子领取,无需架构变更;
  - API Key 隔离与 30/h 节奏两条保留。
- **§6.2 sync_queue 表**:新增 locked_at / locked_by;status 枚举更新。
- **§6.3**:新增索引 sync_queue(status, locked_at)(超时恢复扫描)。
- 仍为 PostgreSQL + sync_queue 状态机,未引入 Redis/BullMQ。

## B3:visitor_id 与本地收藏彻底分离

- **§10.1**:删除 POST /api/favorites 端点,替换为"本地收藏(冻结版 §13,无服务端端点)":P0 收藏 = localStorage/IndexedDB 纯前端本地数据,不写服务器数据库、无任何服务端端点;visitor_id 仅用于匿名、非敏感行为统计,禁止参与收藏;禁止 P0 形成"匿名用户收藏数据库";P1 账号体系后云端收藏 + 本地迁移。
- **§6.1 user_events**:event_type 去掉 favorite/unfavorite(收藏不产生服务端事件)。
- **§6.1 website_metrics**:month_favorites 标注仅 P1 账号体系后启用。
- **§5.3**:notifier(P1)改为基于账号体系收藏,不基于 visitor_id。
- **§2 架构图 / 模块职责表**:Public API 移除 /api/favorites;Reason Builder、Notifier 职责说明更新。
- **§13 M10 / §14 风险 #8 / §15 检查项 9**:同步更新(收藏率指标依赖 P1 账号体系)。

---

# 18. 修订后状态

1. **5 项修复全部回写正文**(无单独补丁代替正文),修订记录见 #17。
2. **重新内部一致性检查通过**:#15 检查项 1-13 全部 ✅(含新增的 tag_type 四枚举、推荐落库分层、sync_queue 防重三项)。
3. **P0 范围未扩大、产品语义未修改**:未引入 Redis/Elasticsearch/BullMQ/Docker/微服务/复杂 RBAC/账号体系;SiteIntel 仍只经 API + 同步契约通信。
4. **保持候选版,不冻结**:技术架构 V1.0-rev2 候选版(已与 Schema V1.0 冻结版完成基线同步,见 #19),待 API 契约阶段复核。

---

# 19. V1.0-rev2 基线同步记录(与 Schema V1.0 冻结版)

> 2026-08-17:《数据库Schema与数据同步契约-V1.0-冻结版》正式冻结后,将已冻结的字段级决策回写本架构文档(基线同步,非重新设计)。**架构仍为候选版,不冻结。**

| # | 同步项 | 回写位置 |
|---|---|---|
| 1 | tasks.aliases JSON → 独立表 task_aliases(alias_normalized 全局唯一;词典载入内存与 Top-1 ≥85% 验收不变) | §6.1 新增表定义;§7.2 词典构建;§9.1 admin/tasks;§10.1 aliases_count 来源;§10.2 admin API |
| 2 | related_tag_slugs JSONB → 独立表 task_related_tags(仅辅助搜索意图识别与 Constraint Mapper,不替代 website_tasks) | §6.1 新增表定义;§9.1 admin/tasks |
| 3 | status_signals JSONB → 独立表 website_changes(四层语义:snapshots=历史摘要 / status=当前聚合 / metrics=长期累计 / changes=结构化事件,不合并) | §4.1 步骤 8;§5.3;§6.1 删除 status_signals 并新增表定义 |
| 4 | normalized_domain 为唯一实体键(domain 仅原始输入/展示/日志,不承担唯一性/去重/关联/幂等) | §6.1 websites;§4.2 sync_queue 简表与幂等;§6.2 submissions/sync_queue/sync_logs;§6.3 唯一约束 |
| 5 | sync_queue 冻结规则(7 状态状态机、原子领取、Worker Lock、10 分钟超时恢复、指数退避、幂等、断点续跑)+ 重试边界:attempts 自增后用更新后值判定,attempts >= max_attempts → failed,默认 5,第 5 次失败即 failed,无第 6 次 | §4.2 超时恢复 SQL 两步骤 + 失败/重试/退避;§6.2 sync_queue 表(job_type / max_attempts / normalized_domain);§6.2 settings |
| 6 | recommendations + recommendation_evidence 仅编辑决策/固定推荐集/人工覆盖写入,实时搜索不落库 | 已一致(§8.6 两层职责),无回写 |
| 7 | P0 收藏纯本地(localStorage/IndexedDB),无 favorites 表/服务端端点/账号体系,visitor_id 不参与收藏 | 已一致(§10.1),无回写 |

**附带同步**:website_metrics 删除 month_favorites(冻结版 A2 决定);§6.3 JSON 字段清单同步。

**一致性检查**:task_aliases 已替代 aliases JSON ✅;task_related_tags 已替代 related_tag_slugs ✅;website_changes 已替代 status_signals ✅;normalized_domain 实体规则与冻结版一致 ✅;sync_queue 状态机与冻结版一致 ✅;attempts >= max_attempts 规则一致 ✅;recommendations 不被实时搜索污染 ✅;P0 收藏仍为纯本地 ✅;无新增 Schema 实体(表数 24 不变)✅;P0/P1 边界未改 ✅;仍为候选版 ✅。
