# 网址发现平台 — 前后台信息架构设计 V1.0(候选版)

> 状态:**候选版**;本阶段不写代码、不选组件库,只做"产品功能 → 用户路径 → 页面 → 页面数据 → 冻结 API → 后台管理入口"的信息架构。
> 依据冻结基线:产品 V2.1.2 冻结版 / Schema V1.0 冻结版(24 表)/ API 契约 V1.0 冻结版 / 技术架构 V1.0-rev2(候选)。
> 内部一致性检查见《docs/audit/前后台信息架构-内部一致性检查报告.md》。
> **GAP-01 处置:已裁定 / 已关闭(2026-08-17,方案 B——首页服务端只读直查冻结 Schema,不新增 Public API;依据《docs/audit/GAP-01-首页聚合数据专项审计报告.md》)。**

---

# 1. 设计目标

1. 定义用户从"进入网站"到"完成任务"(发现网站/提交/纠错/收藏)的完整路径与页面承载;
2. 定义 Admin 从登录到管理 13 类对象的完整路径与页面承载;
3. 建立**页面 ↔ 冻结 API ↔ 冻结 Schema** 三层映射,开发阶段"一个页面知道调用哪个 API、一个 API 知道被哪个页面使用";
4. 全程不越过冻结边界:不新增功能/实体/API,不提前 P1,不引入账号体系/RBAC/服务端收藏。

# 2. 输入冻结基线(信息架构输入基线确认)

| # | 基线 | 状态 | 本阶段用途 |
|---|---|---|---|
| 1 | 产品规划 V2.1.2 冻结版 | 已冻结 | P0 用户端 14 项 / 管理端 9 项 / SEO 架构 / Task 治理 / 关系边界 |
| 2 | Schema 与数据同步契约 V1.0 冻结版 | 已冻结 | 页面数据字段来源;24 表只读 |
| 3 | API 契约 V1.0 冻结版 | 已冻结 | 页面 → API 映射唯一依据 |
| 4 | 技术架构 V1.0-rev2 | 候选 | Admin 页面树(§9)/ 缓存 / 部署 |

**P0 用户功能(产品 §12)**:首页 / Category / Task 页 / Website 详情 / 搜索 / Query→Task+Constraint 解析 / 相似网站 / 人工精选替代 / 可解释推荐理由 / 网站状态与变化信号 / 本地收藏 / 用户提交网站 / 数据纠错入口 / 编辑精选·今日推荐。

**P0 Admin 功能(产品 §12 管理端)**:网站管理 / Category 管理 / Task 管理 / Tag 管理 / 提交审核队列 / 纠错处理队列 / 推荐依据维护 / 精选管理 / 基础发布状态管理。

**P1 禁止提前进入 P0**:related 关系(产品 §5)、对比页规模化、账号与云端收藏(产品 §13)、Query Landing Page 批量(产品 §8)、事件增量端点(缺口 2)。

**Public API(11)**:search / tasks/{slug} / websites/{normalized_domain} / websites/{normalized_domain}/relations / categories / categories/{slug} / tags / tags/{slug} / submissions(POST) / reports(POST) / events(POST)。

**Admin API(12 能力域)**:overview / websites(+sync+relations) / website-relations / categories / tasks(+aliases+related-tags) / tags / submissions / reports / recommendations(+supersede) / curated-picks / settings / sync(+manual+retry+skip+logs) / seo。

**实体可展示范围**:详情页只展示导航站自身持久化摘要(Schema §11.2 白名单);evidence.rawData / 原始 DNS 明细 / 完整 IP 明细 / 完整证书链 / SiteIntel 内部字段全部禁止。

# 3. 产品主导航(Public)

```
┌─────────────────────────────────────────────┐
│ Logo · 搜索框(全站常驻)             [提交网站] │
│ 分类 ▾ | 任务精选 | 标签 | 收藏              │
└─────────────────────────────────────────────┘
```

- 主导航 = 搜索框(主路径)+ 分类导航(发现)+ 任务精选(意图发现)+ 收藏(本地);
- 标签不进入一级导航(标签是筛选语义,不是浏览主通道;标签入口在搜索结果页筛选面板与详情页标签展示);
- **主路径判定**:搜索(用户带着需求来)→ 任务/分类(用户来发现)→ 首页首页承担"搜索 + 精选 + 任务入口 + 分类导航"四合一,搜索框为视觉第一元素。

# 4. 前台信息架构

## A. 首页 `/`(P0)

**用户第一步能做什么**:输入需求(搜索框,聚焦态)+ 浏览编辑精选 / 今日推荐 + 进入任务或分类。

页面模块(自上而下):

| 模块 | 内容 | 数据来源 | API |
|---|---|---|---|
| 搜索框(首屏) | 主路径入口,占位提示引导输入"如:免费做PPT的AI网站" | — | GET /api/search(提交后) |
| 编辑精选 / 今日推荐 | curated_picks 已发布条目(站点名+一句话理由+任务上下文) | curated_picks(published) | 服务端只读直查(冻结 Schema) |
| 任务入口 | 可索引任务(seo_indexable)精选网格,含任务名与候选数 | tasks(active & seo_indexable) | 服务端只读直查(冻结 Schema) |
| 分类导航 | 一级分类树(含子分类) | categories | GET /api/categories |
| 页脚 | 关于本站 / 提交网站入口 / 纠错说明(静态) | — | — |

**首页不是功能堆砌**:搜索框首屏聚焦;精选/任务/分类为三类发现方式并排,优先级搜索 > 任务 > 分类。

**首页直查护栏(已裁定,2026-08-17)**:首页服务端直查**仅允许**上述冻结数据来源——curated_picks(published,按 sort_order)+ tasks(active & seo_indexable)共 2 个只读查询;分类导航必须经 GET /api/categories;**不读取、不写入 recommendations**;零新增 Schema 表/字段/索引;零新增 Public API;未来新增首页聚合需求**不得自动直查**,必须重新走 API Change Request 或专项裁定(依据:《docs/audit/GAP-01-首页聚合数据专项审计报告.md》)。

## B. 搜索结果页 `/search?q=…`(P0,noindex)

| 要素 | 行为(严格按冻结 API 契约 §4.1) |
|---|---|
| 输入 | `q`(1~200 字符);空 q 前端拦截 → 400 VALIDATION_ERROR 提示 |
| Task 识别 | alias 词典(全等 > 前缀/包含 > 多 Token);Top-1 输出任务卡 + confidence 展示 |
| Constraint | Mapper 提取约束标签;任务命中后 task_related_tags 辅助意图扩展(仅辅助,不替代 website_tasks) |
| 结果列表 | 每项:站点名(display_name)/ 一句话描述 / 推荐理由卡片(可展开 reason_expand)/ 状态信号(最近检测正常访问·检测时间) |
| 排序解释 | 五因子加权排序,推荐理由逐条可解释("与'制作PPT'任务匹配""支持免费使用");**无黑盒** |
| 筛选面板 | 按约束标签筛选(`tag` 参数,逗号分隔);标签来源 GET /api/tags(仅 attribute/feature/constraint/scenario 四枚举) |
| 空结果 | 200 空列表 + "调整关键词/浏览任务"引导(热门任务入口) |
| 进入详情 | 点击条目 → /website/{normalized_domain} |
| 埋点 | 点击结果上报 search_click;理由展开上报 reason_expand;搜索落 search_logs(服务端) |

**搜索算法冻结**:本页不做任何算法级设计,只做展示与交互映射。

## C. Task / 任务发现页

- **入口**:首页任务入口(精选网格)→ /task/{slug}。
- **任务页 `/task/{slug}`(P0,SEO 可索引)**:任务名(H1)+ 用户需求说明(description)+ 候选网站列表(固定推荐集优先,五因子排序)+ 每项推荐理由 + 状态信号 + 可索引门槛(候选 ≥5 且 ≥3 完整推荐依据,未达门槛 → 404,与 API 一致)。
- **任务筛选**:P0 不提供 constraint 筛选交互(API 契约 §4.2 无 tag 参数;筛选能力由搜索页承担);参数页 noindex 规则(产品 §8)留给 P1 增强。
- **任务聚合索引 `/tasks`(P1)**:任务量达标后的发现型聚合页(可索引任务列表);**P1 开发时单独确定数据访问方式**(不得自动继承首页直查权限;如需新增 Public API 走 API Change Request)。

## D. 分类页

- **分类导航**:首页分类树 + 全站导航下拉(GET /api/categories)。
- **分类页 `/category/{slug}`(P0,SEO 可索引)**:分类说明(H1,区别于 Task 的"这一类网站是什么")+ 子分类 + 分类内网站列表(同 search items 结构)。
- 与任务页**防重复**(产品 §8):不互 canonical、H1 不同、结构不同。

## E. 标签浏览 / 筛选

- 标签展示:详情页标签区 + 搜索结果页筛选面板 + 任务页候选标签角标;
- 标签筛选:搜索结果页 `tag` 参数(多选,逗号拼接);筛选面板只展示四枚举标签(attribute/feature/constraint/scenario),**tags.group 不进入任何公开匹配语义**(仅后台管理分组);
- **独立标签页 `/tag/{slug}`(P1)**:发现型页(该标签下网站列表),API 已 P0(GET /api/tags/{slug}),页面推迟到 P1。

## F. 网站详情页 `/website/{normalized_domain}`(P0,SEO 可索引)

**路由键 = normalized_domain(实体唯一键,冻结)**。六层信息架构:

| 层 | 内容 | Schema 来源 |
|---|---|---|
| 一、基本信息 | display_name / 一句话描述 / 首页地址 / 所属分类 | websites |
| 二、分类 / 标签 | 主分类 + 标签列表(tag_type 四枚举) | categories / website_tags / tags |
| 三、摘要与状态 | summary_text(zh)/ 状态信号(最近检测正常访问·检测时间·http_status)/ 数据更新时间 | websites.summary_text / website_status |
| 四、推荐理由 | 五因子可解释理由卡片(可展开) | 内存计算(冻结 recommendations 边界,页面不写库) |
| 五、相似 / 替代 | similar / alternative 列表(站点名+判定依据 basis),点击跳新详情 | website_relations(仅两类型) |
| 六、变化信息 | 近期变化时间线(change_type+delta+时间,默认 5 条) | website_changes |

**数据边界(硬规则)**:禁止 evidence.rawData / 原始 DNS 明细 / 完整 IP 明细 / 完整证书链 / SiteIntel 内部字段;禁止"活跃度/运营活跃/业务增长/质量更高"表述;online 只呈现为"最近检测正常访问"。

## G. 相似 / 替代(详情页区块,P0)

- 区块置于第五层;仅渲染 similar / alternative;**related 字段不存在于响应与页面(API 契约 §4.4 p0_gate 冻结)**;
- 点击条目 → 目标站点详情页(路径 4/5);区块数据 GET /api/websites/{normalized_domain}/relations。

## H. 网站提交 `/submit`(P0)

| 步骤 | 行为 |
|---|---|
| 表单 | 域名输入(必填)+ 补充说明(选填,≤500 字符);前端轻校验格式 |
| 提交 | POST /api/submissions(normalized_domain 服务端折叠,客户端不传实体键) |
| 成功 | 201 → 成功态提示"已收到,审核后收录" |
| 重复提交 | 409 DUPLICATE_SUBMISSION → 提示"该网站已在提交/收录流程中" |
| 错误 | 400(格式)/ 429(3/min/IP 限流提示)/ 403(提交入口关闭) |

## I. 网站纠错(详情页内表单,P0)

- 入口:详情页"数据纠错"按钮(路径 6);
- 表单:字段下拉 = 冻结白名单 10 项(display_name/description/category_id/homepage_url/online/http_status/technologies/ssl_expires_at/cdn/hosting)+ 期望值 + 说明;
- 提交:POST /api/reports(匿名,无需登录);成功提示"已提交审核";
- 流程:管理端纠错队列处理(accept 按白名单回写 / reject / resolve)。

## J. 收藏(P0,纯前端)

- 详情页收藏按钮(加/移除)+ 导航收藏入口;
- 数据:localStorage / IndexedDB(前端决定);**无服务端表 / 无收藏 API / 无登录 / 无用户中心 / 无同步**;
- **收藏列表页 `/favorites`(P0)**:纯前端渲染本地收藏(站点名/理由/时间),点击跳详情;跨设备不同步(产品 §13)。

# 5. Admin 信息架构

**入口**:`/admin/login`(邮箱+scrypt+JWT session cookie;ADMIN_EMAILS 白名单交集校验;单角色,无 RBAC)→ 登录后 `/admin`。

**Admin 主导航(侧边栏,13 管理对象)**:

```text
/admin
├── 总览 Dashboard
├── 网站 Websites(列表/新建/详情)
│     └── 子模块:网站关系 Website Relations(详情内)
├── 分类 Categories
├── 任务 Tasks(列表/新建/编辑)
│     └── 子模块:任务别名 Task Aliases(编辑内)
├── 标签 Tags
├── 提交审核 Submissions
├── 纠错队列 Reports
├── 推荐依据 Recommendations
├── 精选管理 Curated Picks
├── 同步队列 Sync Queue(含日志)
├── SEO 索引状态
└── 设置 Settings
```

**13 对象逐项定义**(用途/入口/列表/详情/创建/编辑/状态操作/API/实体):

| # | 对象 | 页面 | 操作 | API | Schema 实体 |
|---|---|---|---|---|---|
| 1 | Dashboard | /admin | 待审提交/待处理纠错/同步队列摘要/索引状态摘要(只读) | GET /api/admin/overview | 聚合读 |
| 2 | Websites | /admin/websites(列表)、/admin/websites/new(创建)、/admin/websites/[id](详情+编辑) | 列表筛选(status/is_indexed/搜索)/ 新建(category+tags+tasks 关联)/ 编辑(白名单字段,normalized_domain 不可改)/ 状态流转 draft→active→archived / 同步按钮 / 索引开关 | GET/POST /api/admin/websites、GET/PATCH /api/admin/websites/{id}、POST /api/admin/websites/{id}/sync | websites + 关联表 |
| 3 | Website Relations | /admin/websites/[id](子模块) | 创建 similar/alternative(白名单校验,related → 422)/ 失效 superseded / 修改 basis | POST /api/admin/websites/{id}/relations、PATCH /api/admin/website-relations/{id} | website_relations |
| 4 | Categories | /admin/categories(树+新建+编辑) | 新建/改名/改 slug/父子调整/排序/隐藏(环检测) | GET/POST /api/admin/categories、PATCH /api/admin/categories/{id} | categories |
| 5 | Tasks | /admin/tasks(列表)、/admin/tasks/new、/admin/tasks/[id](编辑) | 新建(name/slug/description)/ 编辑 / 状态 draft→active→archived / 候选数与索引状态展示 | GET/POST /api/admin/tasks、PATCH /api/admin/tasks/{id} | tasks |
| 6 | Task Aliases | /admin/tasks/[id](子模块) | 别名整体替换(旧行 hidden 停用 + 新行插入,不硬删)/ 冲突提示(409) | POST /api/admin/tasks/{id}/aliases | task_aliases |
| 7 | Tags | /admin/tags(列表+新建+编辑) | 新建(tag_type 四枚举,无第五种)/ 改名 / **合并**(迁移引用后归档)/ 隐藏 | GET/POST /api/admin/tags、PATCH /api/admin/tags/{id} | tags + website_tags + task_related_tags |
| 8 | Recommendations | /admin/recommendations(列表+编辑) | 创建(website×task×context,evidence 五枚举)/ 编辑评分与依据 / supersede(不硬删) | GET/POST /api/admin/recommendations、PATCH /api/admin/recommendations/{id}、POST /api/admin/recommendations/{id}/supersede | recommendations + recommendation_evidence |
| 9 | Submissions | /admin/submissions(审核队列) | 列表(status 筛选)/ 批准(建 website 草稿 source=submission)/ 拒绝 / 标记重复(review_note) | GET /api/admin/submissions、POST /api/admin/submissions/{id} | submissions + websites |
| 10 | Reports | /admin/reports(纠错队列) | 列表(status 筛选)/ 接受(按 field 白名单回写 + resolved_value 留痕)/ 拒绝 / 解决 | GET /api/admin/reports、POST /api/admin/reports/{id} | reports + websites |
| 11 | Sync Queue | /admin/sync(队列)、/admin/sync/logs | 队列状态(attempts/max_attempts/next_retry_at/last_error)/ 手动入队(priority=-10)/ 重试(failed→pending,attempts 清零)/ 跳过(skipped)/ 日志查看(30 天) | GET /api/admin/sync、POST /api/admin/sync/manual、POST /api/admin/sync/{id}/retry、POST /api/admin/sync/{id}/skip、GET /api/admin/sync/logs | sync_queue + sync_logs |
| 12 | SEO Index Status | /admin/seo | 索引状态清单(低于阈值自动降级项,候选数/完整依据数/is_indexed) | GET /api/admin/seo | tasks 聚合 |
| 13 | Settings | /admin/settings | 权重(ranking/quality)/ task 阈值 / constraint 词典 / retention / sync_queue.max_attempts(≥1) | GET/PATCH /api/admin/settings | settings |

**Admin 页面不新增任何接口**(13 对象全部落在冻结 Admin API 内);推荐只 superseded、Tag/Category 只归档——Admin 无删除按钮。

# 6. Public 用户路径(8 条)

```
路径 1(搜索主链路):
  首页(搜索框) → /search?q=… → 结果列表(推荐理由展开/约束筛选) → /website/{normalized_domain}
  埋点: search_logs · search_click · reason_expand · visit

路径 2(Task 意图发现):
  首页(任务入口) → /task/{slug} → 候选网站(推荐理由) → /website/{normalized_domain}
  埋点: visit · reason_expand

路径 3(分类浏览):
  首页(分类导航) → /category/{slug} → 分类内网站 → /website/{normalized_domain}

路径 4(similar):
  /website/A → 详情第五层 similar → /website/B

路径 5(alternative):
  /website/A → 详情第五层 alternative → /website/B

路径 6(纠错):
  /website/{normalized_domain} → "数据纠错" → POST /api/reports → 成功提示
  (管理端:/admin/reports 处理 → accept 回写)

路径 7(提交网站):
  首页([提交网站]) → /submit → POST /api/submissions → 成功/409/错误提示
  (管理端:/admin/submissions 审核 → 批准转建草稿)

路径 8(本地收藏):
  /website/{normalized_domain}(收藏按钮) → localStorage/IndexedDB → /favorites(收藏列表) → 跳回详情
  无服务端参与;P1 账号体系前不同步
```

# 7. Admin 管理路径

```
路径 A(日常内容治理):
  /admin/login → /admin(总览) → /admin/websites | /admin/tasks | /admin/categories | /admin/tags → 列表 → 详情/编辑 → 保存(revalidate 触发)

路径 B(提交审核闭环):
  /admin → /admin/submissions → 批准(转建草稿)→ 去 /admin/websites 完善 → 发布(active)

路径 C(纠错处理闭环):
  /admin → /admin/reports → 接受(按白名单回写)→ 状态 resolved

路径 D(关系维护):
  /admin/websites/[id](网站详情)→ 关系子模块 → 创建 similar/alternative / supersede

路径 E(推荐治理):
  /admin/recommendations → 创建/编辑推荐+evidence → 发布(任务页 revalidate)

路径 F(同步运维):
  /admin → /admin/sync(队列)→ 手动入队 / retry / skip → /admin/sync/logs 排障
  /admin/seo(索引状态)→ 低于阈值任务查看候选数 → 回内容治理补足
```

# 8. 页面清单(页面矩阵)

## 8.1 Public 页面(12 = 功能 11 + 基建 1)

| 页面 | 路由 | 用户 | 页面目标 | P0/P1 | 数据来源 | API | 写操作 |
|---|---|---|---|---|---|---|---|
| 首页 | / | 匿名 | 主入口:搜索/精选/任务/分类 | P0 | curated_picks、tasks(seo_indexable)、categories | 服务端只读直查 curated_picks/tasks + GET /api/categories(护栏见 §4.A) | events |
| 搜索结果页 | /search?q=… | 匿名 | Query→Task+Constraint→候选→推荐理由(noindex) | P0 | 内存计算 + 冻结库 | GET /api/search | search_logs(服务端)、events |
| 任务页 | /task/[slug] | 匿名 | SEO 实体页:候选+推荐理由+状态信号 | P0 | website_tasks、recommendations(固定集)、status | GET /api/tasks/{slug} | events |
| 分类页 | /category/[slug] | 匿名 | SEO 实体页:分类内网站列表 | P0 | categories、websites、status | GET /api/categories/{slug} | events |
| 网站详情页 | /website/[normalized_domain] | 匿名 | 六层信息架构 | P0 | 10 表摘要 | GET /api/websites/{normalized_domain}、/relations | events、POST /api/reports |
| 提交网站页 | /submit | 匿名 | 提交新网站 | P0 | 用户输入 | POST /api/submissions | submissions(写) |
| 收藏列表页 | /favorites | 匿名(本地) | 查看本地收藏 | P0 | localStorage/IndexedDB | 无 | 无(本地写) |
| 404 | /(not-found) | 匿名 | 兜底 + 引导(提交/搜索) | P0(基建) | — | — | — |
| 标签页 | /tag/[slug] | 匿名 | 标签下网站列表(发现型) | P1 | tags、website_tags、websites | GET /api/tags/{slug} | events |
| 任务聚合索引 | /tasks | 匿名 | 可索引任务列表(发现型) | P1 | tasks(seo_indexable) | P1 开发时单独确定数据访问方式(见 §4.A 护栏) | — |
| 精选专题页 | /topic/[slug] | 匿名 | 专题内容(SEO 优先级 5) | P1 | curated_picks 专题化 | P1 开发时单独确定数据访问方式(见 §4.A 护栏) | — |
| 对比页 | /compare | 匿名 | 两站对比(产品 §5 P1) | P1 | 详情同源摘要 | GET /api/websites/{normalized_domain}(×2) | — |

## 8.2 Admin 页面(17 = 登录 1 + 功能 16)

| 页面 | 路由 | 目标 | P0/P1 | API | 写操作 |
|---|---|---|---|---|---|
| 登录页 | /admin/login | 单角色登录(ADMIN_EMAILS) | P0 | (session 登录) | session |
| 总览 | /admin | 待审/待处理/同步/索引摘要 | P0 | GET /api/admin/overview | 否 |
| 网站列表 | /admin/websites | 列表/筛选 | P0 | GET /api/admin/websites | 否 |
| 网站新建 | /admin/websites/new | 创建(含关联) | P0 | POST /api/admin/websites | 是 |
| 网站详情 | /admin/websites/[id] | 详情+编辑+状态+同步+关系子模块+纠错记录 | P0 | GET/PATCH /api/admin/websites/{id}、POST …/{id}/sync、POST …/{id}/relations、PATCH /api/admin/website-relations/{id} | 是 |
| 分类管理 | /admin/categories | 树+新建+编辑 | P0 | GET/POST /api/admin/categories、PATCH …/{id} | 是 |
| 任务列表 | /admin/tasks | 列表(候选数/索引状态) | P0 | GET /api/admin/tasks | 否 |
| 任务新建 | /admin/tasks/new | 创建 | P0 | POST /api/admin/tasks | 是 |
| 任务编辑 | /admin/tasks/[id] | 编辑+别名子模块+关联标签子模块 | P0 | PATCH /api/admin/tasks/{id}、POST …/{id}/aliases、POST …/{id}/related-tags | 是 |
| 标签管理 | /admin/tags | 列表+新建+编辑+合并 | P0 | GET/POST /api/admin/tags、PATCH …/{id} | 是 |
| 提交审核 | /admin/submissions | 审核队列(批准/拒绝/重复) | P0 | GET /api/admin/submissions、POST …/{id} | 是 |
| 纠错队列 | /admin/reports | 处理队列(接受/拒绝/解决) | P0 | GET /api/admin/reports、POST …/{id} | 是 |
| 推荐维护 | /admin/recommendations | 创建/编辑/supersede | P0 | GET/POST /api/admin/recommendations、PATCH …/{id}、POST …/{id}/supersede | 是 |
| 精选管理 | /admin/curated-picks | 列表+编辑+发布 | P0 | GET/POST /api/admin/curated-picks、PATCH …/{id} | 是 |
| 设置 | /admin/settings | 权重/阈值/词典/retention/max_attempts | P0 | GET/PATCH /api/admin/settings | 是 |
| 同步队列 | /admin/sync、/admin/sync/logs | 队列监控+手动+retry+skip+日志 | P0 | GET /api/admin/sync、POST …/manual、POST …/{id}/retry、POST …/{id}/skip、GET …/logs | 是 |
| SEO 索引状态 | /admin/seo | 降级清单 | P0 | GET /api/admin/seo | 否 |

# 9. Page → API Mapping

## 9.1 Public

| 页面 | 页面模块 | API | 请求方式 | 数据用途 | 写/读 |
|---|---|---|---|---|---|
| 首页 | 分类导航 | GET /api/categories | GET | 分类树渲染 | 读 |
| 首页 | 精选/任务入口 | 服务端只读直查 curated_picks/tasks(冻结 Schema;护栏见 §4.A) | — | 精选推荐+任务列表 | 读 |
| 搜索页 | 全页 | GET /api/search | GET | 结果/任务/约束/排序理由 | 读(+search_logs 服务端写) |
| 搜索页 | 筛选面板 | GET /api/tags | GET | 四枚举标签列表 | 读 |
| 任务页 | 全页 | GET /api/tasks/{slug} | GET | 任务信息+候选+理由+状态 | 读 |
| 分类页 | 全页 | GET /api/categories/{slug} | GET | 分类+网站列表 | 读 |
| 详情页 | 主体 | GET /api/websites/{normalized_domain} | GET | 六层信息 | 读 |
| 详情页 | 相似/替代区块 | GET /api/websites/{normalized_domain}/relations | GET | similar/alternative 列表 | 读 |
| 详情页 | 纠错表单 | POST /api/reports | POST | 提交纠错 | 写 |
| 提交页 | 表单 | POST /api/submissions | POST | 提交网站 | 写 |
| 收藏页 | 全页 | 无(纯前端本地) | — | 本地渲染 | 本地写 |
| 全站 | 行为埋点 | POST /api/events | POST | visit/reason_expand/submit/search_click | 写 |
| 标签页(P1) | 全页 | GET /api/tags/{slug} | GET | 标签+网站列表 | 读 |

## 9.2 Admin(全部 17 端点组 → 管理入口,逐项见 §5 表与 §8.2;此处列端点→页面)

| Admin API | 使用页面 |
|---|---|
| GET /api/admin/overview | /admin |
| GET/POST /api/admin/websites | /admin/websites、/admin/websites/new |
| GET/PATCH /api/admin/websites/{id} | /admin/websites/[id] |
| POST /api/admin/websites/{id}/sync | /admin/websites/[id](同步按钮) |
| POST /api/admin/websites/{id}/relations、PATCH /api/admin/website-relations/{id} | /admin/websites/[id](关系子模块) |
| GET/POST /api/admin/categories、PATCH /api/admin/categories/{id} | /admin/categories |
| GET/POST /api/admin/tasks、PATCH /api/admin/tasks/{id} | /admin/tasks、/admin/tasks/new、/admin/tasks/[id] |
| POST /api/admin/tasks/{id}/aliases、POST /api/admin/tasks/{id}/related-tags | /admin/tasks/[id](子模块) |
| GET/POST /api/admin/tags、PATCH /api/admin/tags/{id} | /admin/tags |
| GET /api/admin/submissions、POST /api/admin/submissions/{id} | /admin/submissions |
| GET /api/admin/reports、POST /api/admin/reports/{id} | /admin/reports |
| GET/POST /api/admin/recommendations、PATCH /api/admin/recommendations/{id}、POST /api/admin/recommendations/{id}/supersede | /admin/recommendations |
| GET/POST /api/admin/curated-picks、PATCH /api/admin/curated-picks/{id} | /admin/curated-picks |
| GET/PATCH /api/admin/settings | /admin/settings |
| GET /api/admin/sync、POST /api/admin/sync/manual、POST /api/admin/sync/{id}/retry、POST /api/admin/sync/{id}/skip、GET /api/admin/sync/logs | /admin/sync、/admin/sync/logs |
| GET /api/admin/seo | /admin/seo |

# 10. Page → Schema Mapping(页面数据来源)

| 页面 | 页面数据 | Schema 实体 | 字段范围 | 是否允许展示 |
|---|---|---|---|---|
| 首页 | 分类树 | categories | slug/name/children(active) | ✅ |
| 首页 | 精选/任务 | curated_picks / tasks | published 条目(按 sort_order)/ active & seo_indexable 任务 | ✅(服务端只读直查,仅上述两表) |
| 搜索页 | 结果条目 | websites | normalized_domain/domain/display_name/slug/description | ✅ |
| 搜索页 | 状态信号 | website_status | online/http_status/checked_at | ✅ |
| 任务页 | 任务信息 | tasks | name/slug/description/aliases_count | ✅ |
| 任务页 | 候选列表 | website_tasks + websites + website_status | 同搜索条目 + match_basis/override_score 排序依据 | ✅(match_basis 可作推荐理由素材) |
| 分类页 | 分类+网站 | categories + websites + website_status | slug/name/description + 条目字段 | ✅ |
| 详情页 | 六层信息 | websites/categories/website_tags/tags/website_tasks/website_relations/website_status/website_changes/website_metrics/recommendations(只读参考) | 见 §4.F 分层表 | ✅(仅白名单摘要) |
| 详情页 | **禁止展示** | — | evidence.rawData/原始 DNS 明细/完整 IP 明细/完整证书链/SiteIntel 内部字段(insights/audit 原样/history 全量) | ❌ 硬禁止 |
| 提交页 | 提交 | submissions | domain(用户输入)/note | ✅(仅回执,不展示他人数据) |
| 纠错 | 纠错 | reports | field 白名单 10 项/期望值 | ✅(仅当前站点) |
| 收藏页 | 本地收藏 | 无(localStorage/IndexedDB) | — | ✅ 纯前端 |
| Admin 各页 | 管理数据 | 24 表按 §5 逐项 | 管理字段全量(白名单) | ✅(登录后) |

# 11. P0/P1 页面边界

- **P0 页面(Public 7 + Admin 16 + 登录 1 + 404 基建)**:首页/搜索/任务页/分类页/详情页/提交页/收藏页 + Admin 全部;与产品 §12 P0 用户端 14 项一一对应。
- **P1 页面(4)**:标签页 /tag/[slug]、任务聚合 /tasks、专题页 /topic/[slug]、对比页 /compare——均为发现型增强,不进入 P0 开发范围;**无 P1 页面混入 P0**。
- related 在 P0 无任何页面/区块/入口(响应无字段,页面无渲染分支)。

# 12. 页面间跳转关系(摘要)

```text
首页 ──→ /search(提交 q)──→ 详情页
首页 ──→ /task/[slug] ──→ 详情页
首页 ──→ /category/[slug] ──→ 详情页
首页 ──→ /submit
首页 ──→ /favorites ──→ 详情页
详情页 ──similar/alternative──→ 详情页
详情页 ──纠错表单──→(POST /api/reports)
详情页 ──收藏按钮──→(本地写)──→ /favorites
详情页 ──管理入口(登录后)──→ /admin/websites/[id]
任务页/分类页 ──条目──→ 详情页
搜索页 ──标签筛选──→ 搜索页(新参数)
```

# 13. 信息架构 Gap(登记,不改冻结 API)

| # | Gap | 触发页面 | 需求说明 | 处置候选 | 是否阻塞 P0 |
|---|---|---|---|---|---|
| GAP-01 | **首页聚合数据无公开端点**(编辑精选/今日推荐 + 可索引任务列表) | 首页 | 产品 §12 P0 用户端含"编辑精选/今日推荐";首页任务入口需 seo_indexable 任务列表;现有 API:GET /api/search(需 q)、GET /api/tasks/{slug}(单任务)、GET /api/categories(分类)——均无法直接承载精选推荐与任务列表聚合 | **已裁定 / 已关闭(2026-08-17):方案 B —— 首页服务端组件只读直查冻结 Schema(curated_picks published 按 sort_order + tasks active & seo_indexable,共 2 个只读查询),不新增 Public API;分类导航继续经 GET /api/categories;零读取/零写入 recommendations;依据《docs/audit/GAP-01-首页聚合数据专项审计报告.md》;直查护栏见 §4.A** | 否(已处置,不阻塞 P0) |

其余页面需求与冻结 API 全部闭合(搜索/任务/分类/标签/详情/relations/提交/纠错/收藏/Admin 13 对象逐一验证通过)。

# 14. 与冻结基线一致性检查(摘要,详见检查报告)

1. 未新增产品功能(P0 页面 = 产品 §12 清单一一对应;P1 页面为产品 P1 声明能力);
2. 未新增 Schema 实体 / 未修改 24 表;
3. 未修改/新增任何 API(唯一 Gap 已裁定关闭,方案 B 零 API 变更);
4. P0/P1 页面边界正确(related/对比/账号均未入 P0);
5. 收藏纯前端、无服务端收藏页面;
6. recommendations 双层边界(详情/任务页只读展示固定推荐集,页面零写入);
7. normalized_domain 为详情页唯一路由键;
8. SiteIntel 数据边界(详情页禁止清单硬规则);
9. Admin 单角色无 RBAC、无删除按钮(软失效语义)。

# 15. 下一阶段输入

1. ~~GAP-01 处置裁定~~——**已裁定(2026-08-17,方案 B,见 docs/audit/GAP-01-首页聚合数据专项审计报告.md)**,P0 首页开发不再受 Gap 阻塞;
2. 每页面细化:组件树、交互状态(加载/空/错误/限流提示)、表单校验规则(信息架构下一层"页面规格",开发阶段产物);
3. P0 开发任务拆解(M1-M10 模块 ↔ 页面 ↔ API 的映射,架构 §13 模块划分);
4. schema.prisma / migrate / API / 前端 / Worker 实现(冻结文档为唯一依据)。
