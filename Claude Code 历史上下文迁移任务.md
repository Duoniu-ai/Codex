# Claude Code 历史上下文迁移任务

现在执行一次“Claude Code → Codex 工作上下文迁移”。

目标：

将最近 Claude Code 已经完成、正在进行、产生的报告、状态文件、架构文件、数据库说明、TODO、Git 信息以及相关历史工作记录，整理并纳入当前 Codex 工作区，使 Codex 后续能够无缝延续之前的开发工作。

## 一、严格遵守

1. 不修改任何项目源代码。
2. 不修改数据库。
3. 不执行部署。
4. 不执行 Git commit / push。
5. 不执行任何生产环境操作。
6. 不删除任何 Claude Code 原有文件。
7. 不因为发现问题而自行修复。
8. 不重新扫描整个项目。
9. 不重复执行 Claude Code 已经完成的任务。
10. 本次只做“读取、整理、建立上下文”。

## 二、需要寻找的历史资料

优先检查当前工作区及已知项目目录中的：

- Claude Code 生成的 `.md` 报告
- `CLAUDE.md`
- `PROJECT_STATE.md`
- `ARCHITECTURE.md`
- `DATABASE.md`
- `TODO.md`
- `CHANGELOG.md`
- 各类 `REPORT.md`
- 审计报告
- 修复报告
- 验收报告
- 部署报告
- 数据库相关报告
- Git 状态/版本相关报告
- 最近任务产生的日志或记录文件

同时寻找 Claude Code 最近工作的相关文件。

已知 SiteIntel Claude Code 项目路径：

`/www/wwwroot/siteintel.cc/`

如果当前 Windows 工作区存在对应映射、下载目录、同步目录或相关项目目录，也进行定位。

## 三、历史上下文重点

重点恢复最近 Claude Code 的：

### SiteIntel

需要识别：

- 最近完成了什么
- 当前生产版本是什么
- 最近 Git commit
- 已完成哪些 P0/P1/P2 修复
- 当前还有哪些 TODO
- 哪些任务已经完成，不要重复执行
- 哪些任务已经分析但尚未执行
- 哪些任务处于 dry-run
- 哪些数据库操作明确暂缓
- 当前数据库状态
- 当前架构状态
- 当前部署状态
- 当前已知问题

特别注意：

此前 SiteIntel 已经存在：

- `SITEINTEL-REMEDIATION-REPORT.md`
- P0/P1/P2 修复工作
- www Entity 合并相关工作
- Organization 历史实体合并相关工作
- Organization 合并此前保持 dry-run，不能因为迁移上下文而自动执行

必须保留这些状态差异。

## 四、其他项目历史

如果发现 Claude Code 最近还处理过其他项目，也分别识别：

- 项目名称
- 项目路径
- 最近完成任务
- 当前状态
- 未完成任务
- 重要报告
- Git 状态
- 部署状态
- 数据库状态

不要把不同项目的上下文混在一起。

## 五、建立 Codex 上下文

读取并整理完成后，在当前工作区建立：

`PROJECT_STATE.md`

用于保存 Claude Code → Codex 的历史状态迁移结果。

内容至少包括：

1. 项目列表
2. 项目路径
3. 最近工作时间
4. 已完成工作
5. 当前进行中的工作
6. 未完成任务
7. 已知问题
8. 数据库状态
9. 部署状态
10. Git 状态
11. 重要报告文件位置
12. 明确禁止重复执行的任务
13. Claude Code 与 Codex 后续工作的衔接说明

如果已经存在 `PROJECT_STATE.md`：

不要覆盖历史内容。

先读取，再增量整理。

## 六、建立历史索引

如果历史文件数量较多，可以建立：

`CODEX_CLAUDE_HISTORY.md`

它不是重新复制所有历史文件，而是建立索引。

格式：

```text
项目
├── 历史工作
├── 重要报告
├── 当前状态
├── 未完成任务
└── 相关文件
```

每个文件只记录：

- 文件路径
- 文件用途
- 时间
- 与当前状态的关系

不要把大量原始文件内容复制进去。

## 七、建立“不要重复做”的状态

必须特别整理：

### 已完成

Claude Code 已经完成的任务，Codex 后续默认不得重新执行。

### 已分析但未执行

保持“待确认”状态。

### Dry-run

保持 dry-run。

不得因为 Codex 重新接手就自动执行。

### 已明确暂缓

保持暂缓。

### 未知

明确标记 UNKNOWN，不允许猜测。

## 八、Token 控制

本次迁移本身也必须遵守低 Token 原则。

不要：

- 全项目 cat
- 全目录递归读取所有文件内容
- 重复读取同一个报告
- 把所有历史报告全文复制到 PROJECT_STATE.md
- 对整个项目重新做一次审计

优先：

```text
文件名
→ 文件路径
→ 文件时间
→ 文件摘要
→ 状态提取
```

只有与当前状态直接相关的文件才读取完整内容。

## 九、最终报告

完成后只报告：

### 1. 找到了哪些 Claude Code 历史资料

### 2. 哪些项目已经成功建立上下文

### 3. 建立/更新了哪些 Codex 状态文件

### 4. 当前每个项目的状态

### 5. 哪些任务已经完成，不应重复执行

### 6. 哪些任务仍然待确认

### 7. 哪些信息无法确认

不要执行任何后续开发任务。

本次任务到“历史上下文迁移完成”为止。

---

## 最终原则

Claude Code 负责过的工作不是丢弃，而是作为 Codex 的历史上下文继续使用。

Codex 必须做到：

**继承历史 → 理解现状 → 避免重复 → 延续未完成工作**

而不是：

**重新扫描 → 重新分析 → 重复修复 → 浪费 Token。**