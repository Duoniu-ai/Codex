# IPIP0 阶段五 P0 产品完整性修复

根据：

`IPIP0-阶段五-全站产品功能完整性审计.md`

现在只执行 P0 修复。

不要进入 P1。

不要新增公开实体页面。

不要修改 IPIP0 已通过的数据模型、Collector、Risk Snapshot、query_log 生命周期。

不要修改：

`D:\phpstudy_pro\WWW\ipquery`

---

## P0-1：服务指纹 / 端口暴露状态完善

当前问题：

首页显示 `-`，用户无法判断到底是：

- 未检测
- 已检测但未发现
- 数据暂不可用
- 功能未开放

先读取当前真实后端/API状态。

根据真实状态建立明确展示：

- 已检测：未发现
- 尚未检测
- 数据暂不可用
- 功能未开放

禁止虚构检测结果。

本阶段暂时不开发新的指纹扫描或端口扫描能力。

目标只是：

> 让用户准确理解当前 `-` 的含义。

复用现有 IPIP0 状态/Tag/UI 组件，不新增一套颜色系统。

---

## P0-2：移动端导航修复

当前：

768px 以下导航直接消失，没有替代入口。

实现真正的移动端导航。

要求：

- 桌面端导航保持现状
- 768px 以下提供汉堡菜单
- 菜单包含当前真实可用入口
- 查询
- 数据说明
- API 参考
- 关于我们
- 语言切换
- Ping / Traceroute 如当前属于建设中状态，继续保持“建设中”语义

语言切换必须正常。

菜单打开/关闭必须正常。

禁止产生横向溢出。

---

## 验收

至少测试：

- Desktop
- 768px
- 390px
- 375px

以及：

- 中文
- 英文

检查：

1. 导航是否可访问
2. 移动菜单是否正常打开/关闭
3. 页面跳转是否正确
4. 语言切换是否正确
5. 指纹/端口状态是否准确
6. 首页查询是否正常
7. Risk / ASN 链接是否正常
8. API 回归是否正常

禁止 Mock Data。

禁止修改数据库结构。

禁止修改 Collector。

禁止修改 ipquery。

完成后生成：

`IPIP0-阶段五-P0产品完整性修复与验收报告.md`

报告必须明确说明：

- P0-1 修复结果
- P0-2 修复结果
- 各状态的真实来源
- 移动端测试结果
- 中英文测试
- API 回归
- 是否还有 P0 遗留问题

P0 全部 PASS 后停止，不要自动进入 P1。