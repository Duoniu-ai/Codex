# 前后台信息架构 — 内部一致性检查报告

> 检查对象:《docs/architecture/前后台信息架构设计-V1.0-候选版.md》
> 检查基线:产品 V2.1.2 冻结版 / Schema V1.0 冻结版 / API 契约 V1.0 冻结版 / 技术架构 V1.0-rev2(候选)
> 检查日期:2026-08-17
> 结论:**15/15 PASS —— 信息架构候选版内部一致性检查全部通过**(GAP-01 已裁定关闭,2026-08-17,方案 B:首页服务端只读直查冻结 Schema,不新增 Public API;依据《docs/audit/GAP-01-首页聚合数据专项审计报告.md》)

---

## 检查 1:页面与产品 P0 用户功能一一对应(P0 用户端 14 项)

| 产品 P0 用户功能(§12) | 页面承载 | 结果 |
|---|---|---|
| 首页 | /(P0) | PASS |
| Category 页 | /category/[slug](P0) | PASS |
| Task 页 | /task/[slug](P0) | PASS |
| Website 详情 | /website/[normalized_domain](P0) | PASS |
| 搜索 | /search(P0) | PASS |
| Query→Task+Constraint 解析 | /search 结果头部(P0,API 契约 §4.1) | PASS |
| 相似网站 | 详情页第五层 similar(P0) | PASS |
| 人工精选替代 | 详情页第五层 alternative(P0) | PASS |
| 可解释推荐理由 | 任务页/搜索页/详情页理由卡片(P0) | PASS |
| 状态与变化信号 | 详情页第三/六层(P0) | PASS |
| 本地收藏 | 收藏按钮 + /favorites(P0,纯前端) | PASS |
| 用户提交网站 | /submit(P0) | PASS |
| 数据纠错入口 | 详情页纠错表单(P0) | PASS |
| 编辑精选·今日推荐 | 首页精选模块 | 服务端只读直查冻结 Schema(已裁定,方案 B) |

**结论:PASS(14/14 全部承载)。**

## 检查 2:页面数据需求是否全部落在冻结 API 覆盖范围

逐页核对(首页/搜索/任务/分类/标签/详情/relations/提交/纠错/收藏/Admin 13 对象),全部闭合(首页聚合由服务端只读直查冻结 Schema 承担,经 GAP-01 专项裁定,方案 B)。→ **PASS。**

## 检查 3:Public API 11 端点全部有页面用途

| Public API | 页面用途 |
|---|---|
| GET /api/search | /search 全页 |
| GET /api/tasks/{slug} | /task/[slug] 全页 |
| GET /api/websites/{normalized_domain} | /website/[domain] 六层主体 |
| GET /api/websites/{normalized_domain}/relations | 详情页相似/替代区块 |
| GET /api/categories | 首页分类导航 + 全站导航 |
| GET /api/categories/{slug} | /category/[slug] 全页 |
| GET /api/tags | 搜索页筛选面板(四枚举) |
| GET /api/tags/{slug} | /tag/[slug](P1 页,API 已 P0) |
| POST /api/submissions | /submit 表单 |
| POST /api/reports | 详情页纠错表单 |
| POST /api/events | 全站埋点(visit/reason_expand/submit/search_click) |

**PASS(11/11 有页面用途,无一悬空)。**

## 检查 4:Admin API 全部有管理入口

17 端点组全部映射到 /admin 页面(见主文档 §9.2 逐项表):overview→总览、websites(+sync/relations)→网站列表/新建/详情、website-relations→详情子模块、categories→分类管理、tasks(+aliases/related-tags)→任务列表/新建/编辑、tags→标签管理、submissions→提交审核、reports→纠错队列、recommendations(+supersede)→推荐维护、curated-picks→精选管理、settings→设置、sync(+manual/retry/skip/logs)→同步队列、seo→SEO 索引状态。**PASS(无悬空 Admin API)。**

## 检查 5:Admin 13 模块全部有页面承载

Dashboard/Websites/Categories/Tags/Tasks/Task Aliases(任务编辑子模块)/Website Relations(网站详情子模块)/Recommendations/Submissions/Reports/Sync Queue/SEO Index Status/Settings —— 13 项全部落地,每项含用途/入口/列表/详情/创建/编辑/状态操作/API/Schema 实体(主文档 §5 表)。**PASS。**

## 检查 6:未新增产品功能

P0 页面 = 产品 §12 清单逐项对应;P1 页面(标签页/任务聚合/专题/对比)均为产品已声明能力(§5 对比、§8 专题与 QLP、标签筛选),无任何页面是产品未定义的功能。首页"搜索框首屏/导航结构"为信息架构布局决策,不构成新功能。**PASS。**

## 检查 7:未新增 / 未修改任何 API

本阶段零端点新增、零字段新增、零路径修改;唯一暴露的首页聚合需求经 GAP-01 专项裁定以方案 B 处置(服务端只读直查,不落地任何 API 变更)。**PASS。**

## 检查 8:未修改 Schema / 无 Schema Change Request

页面数据来源全部落在 24 表冻结字段;收藏页无表承载(纯前端);未提出任何表/字段/枚举变更。**PASS,无 SCR。**

## 检查 9:P0/P1 页面边界正确

- P0 页面:首页/搜索/任务页/分类页/详情页/提交页/收藏页(Public 7)+ Admin 16 + 登录 + 404;
- P1 页面:/tag/[slug]、/tasks、/topic/[slug]、/compare(4 个),均为发现型增强;
- P1 功能(related/对比规模化/账号/QLP 批量/收藏同步)零进入 P0 页面。**PASS。**

## 检查 10:related P1 门控无页面泄漏

详情页 relations 区块仅渲染 similar/alternative(API 契约 §4.4 p0_gate:响应无 related 字段 → 页面无渲染分支);Admin 关系子模块写入 related → 422 防御(API 契约 §5.2);对比页为 P1 页面不承载 related 展示。**PASS。**

## 检查 11:recommendations 双层隔离,页面零写入

任务页/搜索页/详情页均为只读展示(固定推荐集 + 内存计算理由);唯一写通道 Admin §5.8(推荐维护页,same 层级证据五枚举);页面无任何推荐写入路径。**PASS。**

## 检查 12:收藏纯前端,无服务端参与

/favorites 页面数据来源 = localStorage/IndexedDB;无表、无端点、events 白名单无收藏事件;visitor_id 不参与;页面不请求任何服务端收藏数据。**PASS。**

## 检查 13:SiteIntel 数据边界(禁止清单)

详情页展示字段 = Schema §11.2 冻结白名单(10 表摘要);硬禁止:evidence.rawData / 原始 DNS 明细 / 完整 IP 明细 / 完整证书链 / SiteIntel 内部字段;禁止"活跃度/运营活跃"表述,online 呈现为"最近检测正常访问"。Admin 网站详情同样只读导航站持久化摘要。**PASS。**

## 检查 14:normalized_domain 路由键一致性

详情页路由 /website/[normalized_domain]、relations 端点、Admin 网站详情、submissions 幂等、sync 队列关联全部以 normalized_domain 为键;domain 仅展示/日志;无任何页面以 domain 做关联或跳转键。**PASS。**

## 检查 15:8 条用户路径可达性与跳转闭环

路径 1-8 每条起点→终点→API→埋点完整定义(主文档 §6);页面间跳转图(§12)无死链:首页/搜索/任务/分类/详情/提交/收藏/纠错全连通;详情页相似/替代点击可达新详情;收藏页点击回跳详情;404 提供提交/搜索引导。**PASS。**

---

# 结论

**15/15 PASS —— 信息架构候选版内部一致性检查全部通过**(GAP-01 已裁定关闭):

- GAP-01(首页聚合数据无公开端点:编辑精选/今日推荐 + 可索引任务列表)——**已裁定 / 已关闭(2026-08-17)**:方案 B,首页服务端组件只读直查冻结 Schema(curated_picks published 按 sort_order + tasks active & seo_indexable,共 2 个只读查询),不新增 Public API;分类导航经 GET /api/categories;零读取/零写入 recommendations;护栏:直查范围锁定,未来新增首页聚合需求必须重新走 API Change Request 或专项裁定;依据《docs/audit/GAP-01-首页聚合数据专项审计报告.md》;
- 无 Schema Change Request;
- 无 API Change Request 落地;
- 页面矩阵 P0/P1 标记经逐行复核,无 P1 混入 P0;
- 信息架构候选版收口完成,可进入**信息架构最终冻结审计**。
