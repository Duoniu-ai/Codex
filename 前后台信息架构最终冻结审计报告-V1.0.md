# 前后台信息架构 V1.0 — 最终冻结审计报告

> 审计日期:2026-08-17
> 审计对象:《docs/architecture/前后台信息架构设计-V1.0-候选版.md》
> 审计性质:最终冻结前置审计(审计,非开发;不冻结、不修改任何冻结版)
> 审计结论:**PASS WITH FIXES**(0 P0 / 1 P1 / 4 P2;无 SCR、无 ACR)

---

## ① 审计基线

| # | 基线 | 状态 | 审计方式 |
|---|---|---|---|
| 1 | 产品规划 V2.1.2 冻结版 | 已冻结 | 逐文件读取(§3-§10/§12/§19-§22 全文核对) |
| 2 | 数据库 Schema V1.0 冻结版(24 表) | 已冻结 | 逐文件读取(§0-§16 关键节 + §11.2 映射表全文) |
| 3 | API 契约 V1.0 冻结版 | 已冻结 | 逐文件读取(§1-§12 + 文末变更规则全文核对) |
| 4 | 技术架构 V1.0-rev2 | 候选版 | 逐文件读取(§6/§8.6/§9/§10/§11/§13-§15) |
| 5 | 信息架构 V1.0 | 候选版 | 逐文件读取(全文 15 节) |
| 6 | 信息架构内部一致性检查报告 | 15/15 PASS | 逐文件读取 |
| 7 | GAP-01 专项审计报告 | 方案 B 已裁定 | 逐文件读取 |
| 8 | PROJECT_STATUS.md / PROJECT_BOUNDARY.md | 状态文档 | 逐文件读取 |

审计原则:冻结版为最高基线(产品 > Schema > API);架构候选版与冻结版冲突时以冻结版为准;不为通过审计而重新设计;禁止新增任何表/字段/API/功能/组件。

## ② 审计范围

- 前台 12 页面(功能 11 + 404 基建)与 Admin 17 页面(登录 1 + 功能 16)的信息架构;
- 8 条 Public 用户路径 + 6 条 Admin 管理路径;
- Page → API 映射(Public 11 + Admin 17 端点组)与 Page → Schema 映射;
- GAP-01 首页服务端直查边界复核(含 /tasks、/topic 直查权限专项);
- 技术架构 V1.0-rev2 与 Schema/API 冻结版字段语义一致性复核;
- P0/P1 页面边界与 related/similar/alternative 门控;
- 冻结边界(零新增 API / 零新增 Schema / 零功能扩展)。

## ③ 审计方法

48 项审计点逐项核验(A 页面完整性 6 / B 用户路径 6 / C Page→API 4 / D Page→Schema 5 / E GAP-01 专项 5 / F API·Schema 冻结边界 4 / G 技术架构复核 9 / H P0/P1 边界 4 / I 最终冻结条件 5),每项给出 PASS / 问题 / 证据;问题按 P0(阻断冻结)/ P1(必须修复)/ P2(仅记录)分级;全部核验基于文件实际内容,非摘要判断。

## ④ 页面完整性审计(A)

| # | 审计项 | 结果 | 证据 |
|---|---|---|---|
| A-1 | Public 12 页面全部有明确职责 | **PASS** | 信息架构 §8.1 矩阵:每页含路由/用户/目标/P0-P1/数据来源/API/写操作;首页=搜索+精选+任务入口+分类导航,任务页=SEO 实体页,详情页=六层信息架构,无职责重叠 |
| A-2 | Admin 17 页面全部有明确职责 | **PASS** | §8.2 矩阵 + §5 13 对象表(用途/入口/列表/详情/创建/编辑/状态操作/API/实体逐项定义);Aliases/Relations 为子模块归属 tasks/[id] 与 websites/[id],不重复建页 |
| A-3 | P0/P1 页面边界正确 | **PASS** | §11:Public P0 7(首页/搜索/任务/分类/详情/提交/收藏)+ 404 基建;P1 4(/tag/[slug]/tasks//topic/[slug]/compare);Admin 全 P0;无 P1 混入 P0 |
| A-4 | 不存在未定义页面 | **PASS** | 全站页面 = 矩阵 12+17 完整枚举;无页面不在矩阵内 |
| A-5 | 不存在孤立页面 | **PASS** | §12 跳转图全连通:首页→搜索/任务/分类/提交/收藏;详情页→relations 跳转;收藏页→回跳详情;404→提交/搜索引导 |
| A-6 | 不存在重复页面职责 | **PASS** | 首页(聚合)/任务页(意图实体)/分类页(类目实体)/标签页(筛选语义)四者职责正交(产品 §2 三正交);详情页六层唯一承载 |

## ⑤ 用户路径审计(B)

| # | 审计项 | 结果 | 证据 |
|---|---|---|---|
| B-7 | 首页→搜索→Task→Website 完整闭环 | **PASS** | 路径 1:首页搜索框→/search(q 必填,Task 识别 Top-1 卡)→结果列表(理由展开/约束筛选)→详情页;search_logs/search_click/reason_expand/visit 埋点闭环 |
| B-8 | 分类导航闭环 | **PASS** | 路径 3:首页分类导航(GET /api/categories)→/category/[slug]→分类内网站→详情页 |
| B-9 | 提交网站闭环 | **PASS** | 路径 7:首页[提交网站]→/submit→POST /api/submissions(409/429/400 状态提示)→成功回执;管理端路径 B 审核闭环(批准转建草稿→完善→active) |
| B-10 | 纠错闭环 | **PASS** | 路径 6:详情页纠错表单(field 白名单 10 项)→POST /api/reports→成功提示;管理端路径 C(accept 按白名单回写/resolved) |
| B-11 | 收藏保持纯本地 | **PASS** | 路径 8:收藏按钮→localStorage/IndexedDB→/favorites→回跳详情;无服务端表/端点/事件(API §4.12 冻结声明);events 白名单无 favorite/unfavorite(API §4.11) |
| B-12 | 404/空状态/错误状态明确 | **PASS**(附 P2-4) | 404 页(基建+引导);搜索空结果(200 空列表+任务引导,API §4.1);任务页未达阈值 404(API §4.2);提交/纠错 409/429/403 错误映射(§4.H/§4.I);收藏页空态与全局网络/限流错误态属页面规格粒度,信息架构 §15 已声明为开发阶段产物(P2-4) |

## ⑥ Page → API 审计(C)

| # | 审计项 | 结果 | 证据 |
|---|---|---|---|
| C-13 | Public 11 API 全部有页面消费者 | **PASS** | §9.1 逐项:search→/search;tasks/{slug}→/task/[slug];websites/{nd}→详情;websites/{nd}/relations→详情区块;categories→首页+导航;categories/{slug}→分类页;tags→筛选面板;tags/{slug}→/tag(P1);submissions→/submit;reports→纠错表单;events→全站埋点。11/11 无悬空 |
| C-14 | 无页面依赖不存在的 API | **PASS** | §9.1 全部映射端点均在 API 冻结版 §3.1/§3.2 清单内;唯一非 API 数据源(首页精选/任务)已由 GAP-01 裁定为服务端直查 |
| C-15 | Admin API 全部有对应管理入口 | **PASS** | §9.2:17 端点组逐项映射到 /admin 页面(overview/websites+sync+relations/website-relations/categories/tasks+aliases+related-tags/tags/submissions/reports/recommendations+supersede/curated-picks/settings/sync+manual+retry+skip+logs/seo);与 API 冻结版 §3.2 清单一致 |
| C-16 | 无前端绕过冻结 API 访问不允许数据 | **PASS** | 收藏=纯本地零服务端请求;首页精选/任务=服务端组件(RSC)服务端只读直查(非前端浏览器访问);详情页展示字段=API §4.3 白名单;无任何页面直接读 SiteIntel 或 evidence.rawData |

## ⑦ Page → Schema 审计(D)

| # | 审计项 | 结果 | 证据 |
|---|---|---|---|
| D-17 | 页面数据全部有 Schema 来源或明确计算来源 | **PASS** | §10 映射表:每页数据均标注冻结实体与字段范围;搜索排序理由=内存计算(明确计算来源,不落库);首页=curated_picks/tasks 直查;无数据源悬空 |
| D-18 | 无页面依赖不存在字段 | **PASS** | 详情页六层字段=Schema §11.2 白名单(§2.16/§2.5/§2.11/§2.13 逐表核实);changes 无 severity(API §4.3 明示);纠错字段=reports 白名单 10 项(Schema §2.18) |
| D-19 | 未违反 24 表冻结边界 | **PASS** | 页面数据全部落冻结 24 表;无新表/字段/枚举/索引需求(首页直查复用既有 INDEX(status, sort_order)/INDEX(status, seo_indexable)) |
| D-20 | normalized_domain 贯穿 Website 路由及实体查询 | **PASS** | 详情路由 /website/[normalized_domain]、relations 端点、Admin 网站详情、submissions 幂等(API §4.9 折叠派生)、sync 队列关联——全链一致(API §4.3 明示"实体键贯穿原则") |
| D-21 | domain 未重新承担唯一性/实体关联 | **PASS** | domain 仅原始输入/展示/日志;唯一性/去重/关联/幂等全部经 normalized_domain(Schema §5 冻结规则);信息架构无页面以 domain 为跳转键 |

## ⑧ GAP-01 专项复核(E)

| # | 审计项 | 结果 | 证据 |
|---|---|---|---|
| E-22 | 首页服务端直查严格限于 curated_picks + tasks | **PASS** | §4.A 首页模块表:精选/任务入口数据来源 = "服务端只读直查(冻结 Schema)",仅两表;§9.1/§10 同源 |
| E-23 | curated_picks(published+sort_order)、tasks(active+seo_indexable)明确 | **PASS** | §4.A 表:"curated_picks(published)"/"tasks(active & seo_indexable)";§10:"published 条目(按 sort_order)/ active & seo_indexable 任务";与 Schema §2.16/§2.5 状态枚举一致 |
| E-24 | 分类导航仍用 GET /api/categories | **PASS** | §4.A 表、§8.1 首页行、§9.1、护栏注记——四处均为 GET /api/categories |
| E-25 | 首页完全不读写 recommendations | **PASS** | 首页所有模块数据源 = curated_picks/tasks/categories(API)/静态;无任何 recommendations 引用;护栏注记明文"不读取、不写入 recommendations";GAP-01 审计报告 §13 复核零污染 |
| E-26 | 未借 GAP-01 裁定扩大其他页面直查 | **问题(P1-1)**,详见 ⑨ |

## ⑨ /tasks 与 /topic 直查权限专项审计(特别检查)

**检查对象**:信息架构候选版三处表述:

| 位置 | 当前表述 | 判定 |
|---|---|---|
| §C 任务聚合索引 /tasks(P1) | "数据来源 = 服务端只读直查(同首页护栏 §4.A)或按裁定流程新增端点,P1 开发时再行决策" | **不合规** |
| §8.1 /tasks 行 | "服务端只读直查(同首页护栏 §4.A,开发 P1 时裁定)" | **不合规** |
| §8.1 /topic/[slug] 行 | "服务端只读直查(同首页护栏 §4.A,开发 P1 时裁定)" | **不合规** |

**审计判定**:
1. 首页直查权限的唯一来源 = GAP-01 专项裁定(方案 B),裁定范围明确限定"首页"(GAP-01 审计报告 §7 护栏:"直查范围锁定:仅首页 2 个查询;未来任何新聚合/列表需求 → 必须走 API Change Request,不得继续扩大直查面");
2. /tasks、/topic 为 P1 页面,无任何独立冻结依据/专项裁定支持其服务端直查;
3. "同首页护栏"表述构成以首页裁定为理由**默认获得直查权限**,违反 GAP-01 审计报告护栏与"不得自动继承"原则;
4. 但该问题**不构成 P0**:不涉及冻结基线(产品/Schema/API 冻结版零影响),不涉及 P0 页面(两页均为 P1),不影响 P0 开发;属候选版文档表述缺陷。

**最小修复项(P1-1)**:将上述三处表述改为——"**P1 开发时单独确定数据访问方式**(不得自动继承首页直查权限;如新增 Public API 须按 API 契约冻结后变更流程走 API Change Request)"。

## ⑩ P0/P1 边界审计(H)

| # | 审计项 | 结果 | 证据 |
|---|---|---|---|
| H-40 | related 严格 P1 门控 | **PASS** | 三层执行:创建 422(API §5.2 relations 端点白名单)、规则引擎不产出(API §7)、响应无字段(API §4.4 p0_gate + 422 防御);信息架构 §11/§4.G 无 related 渲染分支 |
| H-41 | similar/alternative 保持 P0 allowlist | **PASS** | API §4.4/§6/§7:三通道(创建/生成/展示)仅 similar/alternative;信息架构详情页第五层仅渲染两类型 |
| H-42 | 无 P1 功能提前进入 P0 | **PASS** | §11 边界 + 页面矩阵逐行复核:related/对比规模化/账号与云端收藏/QLP 批量/事件增量端点全部在 P1 侧 |
| H-43 | 无 P0 页面依赖 P1 API/未来功能 | **PASS** | P0 页面数据源 = 冻结 P0 API(§9.1)+ 首页直查(已裁定)+ 纯前端;/compare(P1 页)复用 P0 详情 API×2 属向下兼容,不反向依赖 |

## ⑪ 技术架构 V1.0-rev2 复核(G)+ API/Schema 冻结边界(F)

| # | 审计项 | 结果 | 证据 |
|---|---|---|---|
| G-31 | 信息架构是否依赖架构中未同步/过期字段语义 | **PASS**(附 P2-1/P2-2) | 信息架构字段引用全部指向 Schema/API 冻结版,未依赖架构过期表述;发现架构自身两处过期(见下) |
| G-32 | task_aliases 正确 | **PASS** | 架构 §6.1 独立表语义 = Schema §2.6(alias_normalized 全局唯一/status 停用);信息架构 Admin 别名子模块=整体替换旧行 hidden(API §5.4) |
| G-33 | task_related_tags 正确 | **PASS** | 架构/API 契约(§4.1)/Schema §2.7 三方一致:仅辅助搜索意图扩展,不替代 website_tasks,不落库原则不变 |
| G-34 | website_changes 正确 | **PASS** | 架构 §6.1 = Schema §2.13(四层语义/每站 20 条滑动);API §4.3 delta={field,from,to},无 severity;信息架构第六层=changes 时间线 |
| G-35 | normalized_domain 一致 | **PASS** | 架构 §6.1 websites 实体键与 Schema §5 折叠规则一致;API 契约 §4.3 明示修订为 normalized_domain |
| G-36 | sync_queue 重试边界无冲突 | **PASS** | 架构 §4.2 = Schema §9 = API §9.2 三方逐字一致(6 枚举/attempts 先 +1 再判定/>=max_attempts→failed/默认 5/第 5 次失败即 failed/10min 超时/CAS) |
| G-37 | recommendations 双层边界 | **PASS** | 架构 §8.6 硬规则 = API §6 = Schema §4:定义层仅 Admin §5.8 写通道;执行层(搜索/任务页)内存不落库;信息架构 §11 复核 |
| G-38 | P0 收藏纯本地 | **PASS** | 架构 §10.1 本地收藏冻结声明 = API §4.12 = 信息架构 §4.J;/favorites 无服务端参与 |
| G-39 | 架构候选版是否阻塞信息架构冻结 | **PASS**(无阻塞) | 架构候选版与信息架构零冲突;发现两处架构自身过期表述(与冻结版冲突时以冻结版为准):**P2-1** 架构 §13 M6 详情页仍写"四层信息架构"(信息架构定稿六层);**P2-2** 架构 §10.1 详情响应仍含 `relations.related[]`(与 API 冻结版 §4.4 p0_gate 冲突,须删除,信息架构未继承该字段) |
| F-27 | 是否新增/修改 Public API | **否(PASS)** | 零端点变更;首页直查为服务端内部实现,非 API 面 |
| F-28 | 是否隐含 API Change Request | **否(PASS)** | /tasks、/topic 为 P1 页,其数据访问方式在 P1 开发时单独裁定,不构成 P0 隐含 ACR |
| F-29 | 是否需要 Schema Change Request | **否(PASS)** | 零新增表/字段/枚举/索引;直查复用冻结索引 |
| F-30 | 是否有页面设计反向修改冻结 Schema 倾向 | **否(PASS)** | 信息架构全部为"适配冻结基线"式设计;唯一 Gap 已按冻结流程裁定 |

## ⑫ 发现的问题

### P0(阻断冻结)—— 0 项

无。

### P1(必须修复)—— 1 项

| # | 名称 | 位置 | 问题 | 影响 | 修复 |
|---|---|---|---|---|---|
| P1-1 | /tasks、/topic 直查权限表述不合规 | 信息架构 §C、§8.1 两行 | 以"同首页护栏 §4.A"表述使 P1 页面默认继承首页直查权限;无独立冻结依据支持其服务端直查,违反 GAP-01 审计报告护栏("不得继续扩大直查面")与任务书"不得自动继承"原则 | 若放任,P1 阶段将绕过 API Change Request 流程扩大直查面 | 三处表述改为"**P1 开发时单独确定数据访问方式**(不得自动继承首页直查权限;如需新增 Public API 走 API Change Request)" |

### P2(仅记录)—— 4 项

| # | 名称 | 位置 | 说明 |
|---|---|---|---|
| P2-1 | 架构候选版 M6 "四层信息架构"表述过时 | 架构 §13 | 信息架构定稿为六层;架构复核时同步更新,不阻塞 |
| P2-2 | 架构候选版 §10.1 详情响应含 related[] | 架构 §10.1 | 与 API 冻结版 §4.4 p0_gate 冲突;以 API 冻结版为准,架构复核时删除该字段;信息架构未继承,无实际影响 |
| P2-3 | PROJECT_BOUNDARY.md 阶段描述过时 | PROJECT_BOUNDARY.md | "当前产品阶段"仍为"API 契约设计与接口冻结(候选版)";收口时更新 |
| P2-4 | 细粒度空态/错误态留待页面规格 | 信息架构 §15 | 收藏页空态、全局网络/限流错误提示属页面规格粒度,已声明为开发阶段产物 |

## ⑬ Schema Change Request / API Change Request 判定

- **Schema Change Request:无**(信息架构零触碰 24 表,无新表/字段/枚举/索引需求);
- **API Change Request:无**(零新增/零修改 API;首页直查为服务端内部实现;P1 页面数据访问方式留待 P1 阶段单独裁定)。

## ⑭ 最终结论

**PASS WITH FIXES**

- 48 项审计:47 项 PASS + 1 项 P1 问题(文档表述,零冻结基线影响);
- P0 阻断问题:0;P1 必须修复:1(P1-1 直查权限表述);P2 仅记录:4;
- 修复 P1-1 后(仅改信息架构候选版 3 处表述,零触碰冻结版),具备正式冻结条件;
- 架构 V1.0-rev2 复核结论:与 Schema/API 冻结版无冲突性不一致,两处自身过期表述(P2-1/P2-2)在架构自身复核/冻结时处理,不阻塞本阶段;
- 冻结流程:信息架构候选版修复 → 复检 → 冻结为《前后台信息架构设计-V1.0-冻结版.md》(候选版保留)→ 验收记录 → 进入 P0 开发任务拆解。
