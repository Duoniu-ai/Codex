# IPIP0 首页结果卡片专项修复

本次只针对当前首页查询结果区域及现有导航入口进行修复。

不要重新设计首页，不要修改数据库架构，不要修改 Collector，不要修改阶段四 P0/P1 数据生命周期。

先读取当前真实代码并修复以下问题。

## 1. 删除无价值的结果上下文行

当前：

`IP 情报结果 · {IP}`

用户不需要。

删除该行，不影响查询结果内容。

---

## 2. 修复“眼睛”按钮

当前结果区存在眼睛图标/按钮，但点击没有实际作用。

必须二选一：

- 如果已有真实功能，修复点击行为；
- 如果当前没有真实功能，删除该按钮。

禁止保留无功能交互。

---

## 3. 位置增加国家/地区国旗

当前位置显示国家/地区时，增加对应国旗。

要求：

- 根据真实 country code 动态显示
- 不允许硬编码某个国家
- IPv6 / 国家缺失时有正常 fallback
- 中英文都正常

优先复用当前项目已有组件/样式。

---

## 4. ASN 补充网络类型标识

当前：

`ASN ASxxxxx`

缺少类似：

`ISP`
`Hosting`
`Enterprise`

的类型标识。

要求：

- 先检查当前 API / 数据库是否已经有真实网络类型字段
- 如果已有，直接展示
- 如果没有，不允许前端猜测
- 不允许创建虚假的 ISP / Hosting 判断

### UI 要求

必须复用当前 IPIP0 已存在的 badge/tag 样式。

不要自行创建新的 `ent-badge` 体系。

---

## 5. 修复 IP 类型 Badge 颜色

当前：

```text
数据中心
原生IP
代理网络
```

已有结构，但颜色/样式没有正确呈现。

请检查：

- 当前 badge class
- CSS
- 主题变量
- 中英文 class
- 渲染条件

恢复已有设计系统应有的颜色。

禁止重新设计一套新的颜色体系。

如果项目没有对应颜色 Token，先按现有系统最接近的状态色复用。

---

## 6. 补齐以下字段的视觉标识

以下字段现在缺乏明确的视觉标识：

- IP 标签
- 服务指纹
- 端口暴露

先检查当前项目已有 Badge / Tag / Status 组件。

优先复用。

不要新建第二套 UI 体系。

其中风险相关标签必须继续与风险详情页保持一致。

---

## 7. 增加“适用场景”

在风险/网络属性相关区域增加：

`适用场景`

必须根据真实数据计算。

允许使用：

- IP Type
- IP Tags
- Risk Score
- Risk Tags
- ISP / Hosting / Network Type（仅使用真实字段）
- Native / Anycast 等已有真实字段

建立明确、可解释的规则映射。

例如：

```text
检测结果
↓
场景规则
↓
适用 / 不推荐 / 谨慎
```

禁止使用 AI 随机生成文案。

禁止根据 IP 名称主观猜测。

如果当前真实数据不足，则显示：

`暂无足够数据判断适用场景`

并保留数据依据。

---

## 8. Ping / 路由跟踪不能继续依赖第三方跳转

当前仍存在：

```text
Ping → ping.pe
路由跟踪 → mtr.ping.pe
```

本次先处理为：

### 如果 IPIP0 已有原生能力：

直接接入 IPIP0 原生页面/功能。

### 如果当前没有原生能力：

不要继续跳第三方。

改为：

- 显示“功能建设中”
- 或暂时隐藏入口

不要让核心查询结果把用户导向第三方网站。

同时必须检查代码中是否还存在旧的第三方 URL 硬编码。

---

## 9. 修复语言切换

当前中英文语言切换没有正常工作。

要求完整验证：

```text
/
/en
/en/faq
/en/about
```

以及对应中文页面。

检查：

- language route
- redirect
- locale
- cookie/session
- 当前 URL 保留
- 查询结果页语言切换

要求：

在当前页面点击语言切换后，
进入同一内容的另一语言版本。

如果当前页面：

`/ip/{ip}/risk`

切换语言后必须仍然停留在对应风险详情页。

禁止只跳首页。

---

## 10. 清理旧的 /about 入口

当前页面底部仍存在：

`ipip0.net/about`

先检查当前站点实际是否还需要该页面。

如果 `/about` 是现阶段保留页面：

- 保留并统一导航

如果已经存在新版“关于我们”路径：

- 删除旧入口
- 全站统一到新路径

不要简单删除文件。

必须检查：

- Header
- Footer
- Mobile Menu
- 页面内链接
- sitemap
- 中英文版本

避免出现死链或重复页面。

---

# 开发限制

本次只做：

- 当前结果卡片 UI/UX 修复
- 当前导航/语言功能修复
- Ping/路由入口处理
- 适用场景展示

禁止：

- 修改数据库结构
- 修改 Collector
- 修改 Risk Snapshot
- 修改 query_log
- 修改 ASN Entity 数据模型
- 新增 Organization / Prefix 页面
- 新增 SEO 程序化页面
- 修改 `D:\phpstudy_pro\WWW\ipquery`
- 使用 Mock Data

所有数据必须来自当前真实 API / SQL。

---

# 验收要求

修复后必须使用至少以下真实数据验证：

- 3 个不同国家 IP
- 1 个 IPv6
- 1 个 Hosting / Datacenter
- 1 个 ISP / Residential
- 1 个存在风险标签的 IP

验证：

- 国旗
- ASN 类型
- IP Type Badge
- IP Tags
- Service Fingerprint
- Ports
- 适用场景
- Risk 入口
- ASN 入口
- Ping / Traceroute
- 中英文切换
- `/about` 链接

同时检查：

- 375px
- 390px
- 768px
- Desktop

禁止出现横向溢出。

禁止存在无作用按钮。

禁止出现外部 Ping / Traceroute 跳转。

禁止使用 Mock Data。

最后生成：

`IPIP0-首页结果卡片专项修复与验收报告.md`

报告必须说明：

1. 修复了什么
2. 使用了哪些现有 UI 组件
3. 哪些字段来自真实 API
4. 适用场景规则
5. Ping/Traceroute 最终处理方式
6. 语言切换验证
7. about 链接清理结果
8. 中英文验证
9. 移动端验证
10. API 回归结果
11. Mock Data 检查